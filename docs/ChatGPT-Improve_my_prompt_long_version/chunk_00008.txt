',\n    \"DateOfCall\": r'^\\d{4}-\\d{2}-\\d{2}$|^\\d{1,2}/\\d{1,2}/\\d{4}$',\n}\n\n\nDomain validation fields\n\n\nDOMAIN_FIELDS: Dict[str, Set[str]] = {\n    \"HowReported\": {\n        \"9-1-1\", \"Phone\", \"Walk-In\", \"Self-Initiated\",\n        \"Radio\", \"Online\", \"Referral\", \"Alarm\", \"Other\", \"Transfer\"\n    },\n}\n\n\nAddress quality indicators\n\n\nADDRESS_FIELD = \"FullAddress2\"\nADDRESS_EXCLUSIONS: Set[str] = {\"225 STATE ST\", \"225 State Street\"}\n\n\n=============================================================================\n\n\nScoring Functions\n\n\n=============================================================================\n\n\ndef score_required_fields(\n    df: pd.DataFrame,\n    required_fields: List[str],\n    max_points: float = 30.0,\n) -> Tuple[pd.Series, CategoryScore]:\n    \"\"\"\n    Score records based on required field completeness.\n\n\nArgs:\n    df: DataFrame to score\n    required_fields: List of required field names\n    max_points: Maximum points for this category\n    \nReturns:\n    Tuple of (per-record scores, category summary)\n\"\"\"\n# Find which required fields exist\nexisting_fields = [f for f in required_fields if f in df.columns]\nmissing_fields = [f for f in required_fields if f not in df.columns]\n\nif not existing_fields:\n    # No required fields present - zero score\n    record_scores = pd.Series([0.0] * len(df), index=df.index)\n    return record_scores, CategoryScore(\n        category=ScoreCategory.REQUIRED_FIELDS,\n        max_points=max_points,\n        earned_points=0.0,\n        percentage=0.0,\n        checks_passed=0,\n        checks_failed=len(required_fields),\n        details={\"missing_fields\": required_fields},\n    )\n\n# Calculate per-record completeness\npoints_per_field = max_points / len(required_fields)\n\n# Check each field for non-null, non-empty values\nfield_present = pd.DataFrame(index=df.index)\n\nfor field_name in existing_fields:\n    series = df[field_name]\n    # Present = not null AND not empty string\n    is_present = series.notna()\n    if series.dtype == 'object':\n        is_present = is_present & (series.astype(str).str.strip() != '')\n        is_present = is_present & (series.astype(str).str.lower() != 'nan')\n    field_present[field_name] = is_present.astype(float)\n\n# Sum up points per record\nrecord_scores = field_present.sum(axis=1) * points_per_field\n\n# Penalty for completely missing fields\npenalty_per_missing = points_per_field\nmissing_penalty = len(missing_fields) * penalty_per_missing\n\n# Calculate category summary\ntotal_checks = len(df) * len(required_fields)\nchecks_passed = int(field_present.sum().sum())\nchecks_failed = total_checks - checks_passed + (len(df) * len(missing_fields))\n\nearned = record_scores.mean()\n\n# Field-level details\nfield_details = {}\nfor field_name in existing_fields:\n    present_count = int(field_present[field_name].sum())\n    field_details[field_name] = {\n        \"present\": present_count,\n        \"missing\": len(df) - present_count,\n        \"rate\": round(present_count / len(df) * 100, 2),\n    }\nfor field_name in missing_fields:\n    field_details[field_name] = {\n        \"present\": 0,\n        \"missing\": len(df),\n        \"rate\": 0.0,\n        \"note\": \"Field not in dataset\",\n    }\n\nreturn record_scores, CategoryScore(\n    category=ScoreCategory.REQUIRED_FIELDS,\n    max_points=max_points,\n    earned_points=round(earned, 2),\n    percentage=0.0,  # Will be calculated in __post_init__\n    checks_passed=checks_passed,\n    checks_failed=checks_failed,\n    details={\n        \"fields_checked\": len(required_fields),\n        \"fields_found\": len(existing_fields),\n        \"fields_missing\": missing_fields,\n        \"field_details\": field_details,\n    },\n)\n\n\ndef score_valid_formats(\n    df: pd.DataFrame,\n    format_fields: Dict[str, str],\n    max_points: float = 25.0,\n) -> Tuple[pd.Series, CategoryScore]:\n    \"\"\"\n    Score records based on format validation (regex patterns).\n\n\nArgs:\n    df: DataFrame to score\n    format_fields: Dict of field name -> regex pattern\n    max_points: Maximum points for this category\n    \nReturns:\n    Tuple of (per-record scores, category summary)\n\"\"\"\n# Find which format fields exist\nexisting_fields = {f: p for f, p in format_fields.items() if f in df.columns}\n\nif not existing_fields:\n    record_scores = pd.Series([max_points] * len(df), index=df.index)\n    return record_scores, CategoryScore(\n        category=ScoreCategory.VALID_FORMATS,\n        max_points=max_points,\n        earned_points=max_points,\n        percentage=100.0,\n        checks_passed=0,\n        checks_failed=0,\n        details={\"note\": \"No format fields to validate\"},\n    )\n\npoints_per_field = max_points / len(format_fields)\n\n# Check each field against its pattern\nfield_valid = pd.DataFrame(index=df.index)\n\nfor field_name, pattern in existing_fields.items():\n    series = df[field_name]\n    # Valid = matches pattern OR is null (null handled by required check)\n    is_null = series.isna()\n    matches_pattern = series.astype(str).str.match(pattern, na=False)\n    is_valid = is_null | matches_pattern\n    field_valid[field_name] = is_valid.astype(float)\n\n# Score per record\nrecord_scores = field_valid.sum(axis=1) * points_per_field\n\n# Add points for fields not in dataset (not penalized twice)\nmissing_count = len(format_fields) - len(existing_fields)\nrecord_scores += missing_count * points_per_field\n\n# Category summary\ntotal_checks = len(df) * len(existing_fields)\nchecks_passed = int(field_valid.sum().sum())\n\nearned = record_scores.mean()\n\n# Field-level details\nfield_details = {}\nfor field_name in existing_fields:\n    valid_count = int(field_valid[field_name].sum())\n    field_details[field_name] = {\n        \"valid\": valid_count,\n        \"invalid\": len(df) - valid_count,\n        \"rate\": round(valid_count / len(df) * 100, 2),\n    }\n\nreturn record_scores, CategoryScore(\n    category=ScoreCategory.VALID_FORMATS,\n    max_points=max_points,\n    earned_points=round(earned, 2),\n    percentage=0.0,\n    checks_passed=checks_passed,\n    checks_failed=total_checks - checks_passed,\n    details={\n        \"fields_checked\": len(existing_fields),\n        \"patterns\": {f: format_fields[f] for f in existing_fields},\n        \"field_details\": field_details,\n    },\n)\n\n\ndef score_address_quality(\n    df: pd.DataFrame,\n    address_field: str = \"FullAddress2\",\n    exclusions: Optional[Set[str]] = None,\n    max_points: float = 20.0,\n) -> Tuple[pd.Series, CategoryScore]:\n    \"\"\"\n    Score records based on address quality.\n\n\nQuality factors:\n- Address is present (5 pts)\n- Address is not an exclusion like HQ (5 pts)\n- Address contains street number (5 pts)\n- Address appears properly formatted (5 pts)\n\nArgs:\n    df: DataFrame to score\n    address_field: Name of address field\n    exclusions: Set of addresses to penalize (e.g., HQ address)\n    max_points: Maximum points for this category\n    \nReturns:\n    Tuple of (per-record scores, category summary)\n\"\"\"\nif exclusions is None:\n    exclusions = ADDRESS_EXCLUSIONS\n\nif address_field not in df.columns:\n    record_scores = pd.Series([0.0] * len(df), index=df.index)\n    return record_scores, CategoryScore(\n        category=ScoreCategory.ADDRESS_QUALITY,\n        max_points=max_points,\n        earned_points=0.0,\n        percentage=0.0,\n        checks_passed=0,\n        checks_failed=len(df),\n        details={\"error\": f\"Address field '{address_field}' not found\"},\n    )\n\n# Sub-scores (each worth 1/4 of max points)\nsub_points = max_points / 4\n\naddresses = df[address_field].astype(str).str.strip()\naddresses_upper = addresses.str.upper()\n\n# Check 1: Address is present\nis_present = df[address_field].notna() & (addresses != '') & (addresses.str.lower() != 'nan')\n\n# Check 2: Not an excluded address\nexclusions_upper = {e.upper() for e in exclusions}\nis_not_excluded = ~addresses_upper.isin(exclusions_upper)\n\n# Check 3: Contains street number (starts with digits)\nhas_street_number = addresses.str.match(r'^\\d+\\s', na=False)\n\n# Check 4: Appears properly formatted (has at least 2 words)\nis_formatted = addresses.str.split().str.len() >= 2\n\n# Calculate scores\nrecord_scores = (\n    is_present.astype(float) * sub_points +\n    is_not_excluded.astype(float) * sub_points +\n    has_street_number.astype(float) * sub_points +\n    is_formatted.astype(float) * sub_points\n)\n\n# Category summary\nchecks = {\n    \"present\": int(is_present.sum()),\n    \"not_excluded\": int(is_not_excluded.sum()),\n    \"has_street_number\": int(has_street_number.sum()),\n    \"properly_formatted\": int(is_formatted.sum()),\n}\n\ntotal_checks = len(df) * 4\nchecks_passed = sum(checks.values())\n\nearned = record_scores.mean()\n\n# Count exclusion hits\nexclusion_count = int((~is_not_excluded).sum())\n\nreturn record_scores, CategoryScore(\n    category=ScoreCategory.ADDRESS_QUALITY,\n    max_points=max_points,\n    earned_points=round(earned, 2),\n    percentage=0.0,\n    checks_passed=checks_passed,\n    checks_failed=total_checks - checks_passed,\n    details={\n        \"address_field\": address_field,\n        \"checks\": checks,\n        \"exclusion_hits\": exclusion_count,\n        \"exclusions\": list(exclusions)[:5],  # Limit for readability\n    },\n)\n\n\ndef score_domain_compliance(\n    df: pd.DataFrame,\n    domain_fields: Dict[str, Set[str]],\n    max_points: float = 15.0,\n) -> Tuple[pd.Series, CategoryScore]:\n    \"\"\"\n    Score records based on domain/vocabulary compliance.\n\n\nArgs:\n    df: DataFrame to score\n    domain_fields: Dict of field name -> set of valid values\n    max_points: Maximum points for this category\n    \nReturns:\n    Tuple of (per-record scores, category summary)\n\"\"\"\nexisting_fields = {f: v for f, v in domain_fields.items() if f in df.columns}\n\nif not existing_fields:\n    record_scores = pd.Series([max_points] * len(df), index=df.index)\n    return record_scores, CategoryScore(\n        category=ScoreCategory.DOMAIN_COMPLIANCE,\n        max_points=max_points,\n        earned_points=max_points,\n        percentage=100.0,\n        checks_passed=0,\n        checks_failed=0,\n        details={\"note\": \"No domain fields to validate\"},\n    )\n\npoints_per_field = max_points / len(domain_fields)\n\n# Check each field\nfield_valid = pd.DataFrame(index=df.index)\n\nfor field_name, valid_values in existing_fields.items():\n    series = df[field_name]\n    valid_upper = {v.upper() for v in valid_values}\n    \n    # Valid = in domain OR is null\n    is_null = series.isna()\n    in_domain = series.astype(str).str.upper().str.strip().isin(valid_upper)\n    is_valid = is_null | in_domain\n    field_valid[field_name] = is_valid.astype(float)\n\n# Score per record\nrecord_scores = field_valid.sum(axis=1) * points_per_field\n\n# Add points for missing fields\nmissing_count = len(domain_fields) - len(existing_fields)\nrecord_scores += missing_count * points_per_field\n\n# Category summary\ntotal_checks = len(df) * len(existing_fields)\nchecks_passed = int(field_valid.sum().sum())\n\nearned = record_scores.mean()\n\n# Field-level details with invalid value samples\nfield_details = {}\nfor field_name, valid_values in existing_fields.items():\n    valid_count = int(field_valid[field_name].sum())\n    invalid_mask = ~field_valid[field_name].astype(bool)\n    \n    # Sample invalid values\n    invalid_samples = []\n    if invalid_mask.any():\n        invalid_values = df.loc[invalid_mask, field_name].dropna().unique()[:5]\n        invalid_samples = [str(v) for v in invalid_values]\n    \n    field_details[field_name] = {\n        \"valid\": valid_count,\n        \"invalid\": len(df) - valid_count,\n        \"rate\": round(valid_count / len(df) * 100, 2),\n        \"invalid_samples\": invalid_samples,\n        \"valid_values\": sorted(valid_values)[:10],  # Limit for readability\n    }\n\nreturn record_scores, CategoryScore(\n    category=ScoreCategory.DOMAIN_COMPLIANCE,\n    max_points=max_points,\n    earned_points=round(earned, 2),\n    percentage=0.0,\n    checks_passed=checks_passed,\n    checks_failed=total_checks - checks_passed,\n    details={\n        \"fields_checked\": len(existing_fields),\n        \"field_details\": field_details,\n    },\n)\n\n\ndef score_consistency(\n    df: pd.DataFrame,\n    max_points: float = 10.0,\n) -> Tuple[pd.Series, CategoryScore]:\n    \"\"\"\n    Score records based on cross-field consistency checks.\n\n\nConsistency checks:\n- DateOfCall and TimeOfCall both present if either is\n- Case number year matches DateOfCall year\n- Disposition present if call is closed\n\nArgs:\n    df: DataFrame to score\n    max_points: Maximum points for this category\n    \nReturns:\n    Tuple of (per-record scores, category summary)\n\"\"\"\nchecks_available = 0\nchecks_results = pd.DataFrame(index=df.index)\n\n# Check 1: Date/Time consistency\nif \"DateOfCall\" in df.columns and \"TimeOfCall\" in df.columns:\n    checks_available += 1\n    date_present = df[\"DateOfCall\"].notna()\n    time_present = df[\"TimeOfCall\"].notna()\n    # Both present or both missing = consistent\n    date_time_consistent = (date_present == time_present) | (date_present & time_present)\n    checks_results[\"date_time\"] = date_time_consistent.astype(float)\n\n# Check 2: Case number year matches date year\nif \"ReportNumberNew\" in df.columns and \"DateOfCall\" in df.columns:\n    checks_available += 1\n    \n    def _check_year_match(row):\n        case_num = row.get(\"ReportNumberNew\")\n        date_val = row.get(\"DateOfCall\")\n        \n        if pd.isna(case_num) or pd.isna(date_val):\n            return 1.0  # Can't check, assume OK\n        \n        try:\n            # Extract year from case number (first 2 digits)\n            case_year = int(str(case_num)[:2])\n            if case_year < 100:\n                case_year += 2000\n            \n            # Extract year from date\n            date_str = str(date_val)\n            if '-' in date_str:\n                date_year = int(date_str.split('-')[0])\n            elif '/' in date_str:\n                parts = date_str.split('/')\n                date_year = int(parts[-1]) if len(parts[-1]) == 4 else int(parts[-1]) + 2000\n            else:\n                return 1.0  # Can't parse, assume OK\n            \n            return 1.0 if case_year == date_year else 0.0\n        except (ValueError, IndexError):\n            return 1.0  # Parsing error, assume OK\n    \n    checks_results[\"year_match\"] = df.apply(_check_year_match, axis=1)\n\n# Check 3: Disposition completeness for closed calls\n# (Stub - would need status field to implement fully)\nif \"Disposition\" in df.columns:\n    checks_available += 1\n    # For now, just check disposition is present\n    checks_results[\"disposition_present\"] = df[\"Disposition\"].notna().astype(float)\n\n# Calculate scores\nif checks_available == 0 or checks_results.empty:\n    record_scores = pd.Series([max_points] * len(df), index=df.index)\n    return record_scores, CategoryScore(\n        category=ScoreCategory.CONSISTENCY,\n        max_points=max_points,\n        earned_points=max_points,\n        percentage=100.0,\n        checks_passed=0,\n        checks_failed=0,\n        details={\"note\": \"No consistency checks available\"},\n    )\n\npoints_per_check = max_points / checks_available\nrecord_scores = checks_results.sum(axis=1) * points_per_check\n\n# Category summary\ntotal_checks = len(df) * checks_available\nchecks_passed = int(checks_results.sum().sum())\n\nearned = record_scores.mean()\n\ncheck_details = {}\nfor check_name in checks_results.columns:\n    passed = int(checks_results[check_name].sum())\n    check_details[check_name] = {\n        \"passed\": passed,\n        \"failed\": len(df) - passed,\n        \"rate\": round(passed / len(df) * 100, 2),\n    }\n\nreturn record_scores, CategoryScore(\n    category=ScoreCategory.CONSISTENCY,\n    max_points=max_points,\n    earned_points=round(earned, 2),\n    percentage=0.0,\n    checks_passed=checks_passed,\n    checks_failed=total_checks - checks_passed,\n    details={\n        \"checks_performed\": list(checks_results.columns),\n        \"check_details\": check_details,\n    },\n)\n\n\n=============================================================================\n\n\nQuality Scorer Class\n\n\n=============================================================================\n\n\nclass QualityScorer:\n    \"\"\"\n    Quality scoring engine for CAD/RMS data.\n\n\nCalculates quality scores at both record and dataset levels using\nconfigurable scoring weights and thresholds.\n\nAttributes:\n    weights: Scoring weights by category (must sum to 100)\n    threshold: Minimum acceptable quality score\n    required_fields: List of required field names\n    format_fields: Dict of field -> regex pattern\n    domain_fields: Dict of field -> valid values\n    address_field: Name of address field\n\"\"\"\n\ndef __init__(\n    self,\n    weights: Optional[Dict[ScoreCategory, float]] = None,\n    threshold: float = 80.0,\n    required_fields: Optional[List[str]] = None,\n    format_fields: Optional[Dict[str, str]] = None,\n    domain_fields: Optional[Dict[str, Set[str]]] = None,\n    address_field: str = \"FullAddress2\",\n    address_exclusions: Optional[Set[str]] = None,\n    config_path: Optional[Path] = None,\n):\n    \"\"\"\n    Initialize QualityScorer.\n    \n    Args:\n        weights: Custom scoring weights (uses defaults if None)\n        threshold: Minimum acceptable score (0-100)\n        required_fields: List of required field names\n        format_fields: Dict of field -> regex pattern\n        domain_fields: Dict of field -> valid values\n        address_field: Name of address field\n        address_exclusions: Set of addresses to penalize\n        config_path: Path to validation_rules.yaml for config\n    \"\"\"\n    self.weights = weights or DEFAULT_WEIGHTS.copy()\n    self.threshold = threshold\n    self.required_fields = required_fields or CAD_REQUIRED_FIELDS.copy()\n    self.format_fields = format_fields or FORMAT_VALIDATION_FIELDS.copy()\n    self.domain_fields = domain_fields or DOMAIN_FIELDS.copy()\n    self.address_field = address_field\n    self.address_exclusions = address_exclusions or ADDRESS_EXCLUSIONS.copy()\n    \n    self._results: Optional[QualityResult] = None\n    \n    # Load config if provided\n    if config_path:\n        self._load_config(config_path)\n    \n    # Validate weights sum to 100\n    total_weight = sum(self.weights.values())\n    if abs(total_weight - 100.0) > 0.01:\n        logger.warning(f\"Weights sum to {total_weight}, not 100.