(\\s*[AP]M)?$'\n\n\nDate patterns\n\n\nDATE_PATTERNS = [\n    r'^\\d{4}-\\d{2}-\\d{2}$',           # ISO: 2025-01-30\n    r'^\\d{2}/\\d{2}/\\d{4}$',           # US: 01/30/2025\n    r'^\\d{1,2}/\\d{1,2}/\\d{4}$',       # US flexible\n]\n\n\ndef build_default_cad_rules() -> List[ValidationRule]:\n    \"\"\"\n    Build default validation rules for CAD data based on validation_rules.yaml specs.\n\n\nReturns:\n    List of ValidationRule objects for CAD exports\n\"\"\"\nreturn [\n    # Required fields\n    ValidationRule(\n        field=\"ReportNumberNew\",\n        rule_type=ValidationType.REQUIRED,\n        severity=ValidationSeverity.ERROR,\n        message=\"Case number is required\",\n        description=\"Verify ReportNumberNew is present\",\n    ),\n    ValidationRule(\n        field=\"Incident\",\n        rule_type=ValidationType.REQUIRED,\n        severity=ValidationSeverity.ERROR,\n        message=\"Incident/Call type is required\",\n        description=\"Verify Incident field is present\",\n    ),\n    ValidationRule(\n        field=\"TimeOfCall\",\n        rule_type=ValidationType.REQUIRED,\n        severity=ValidationSeverity.ERROR,\n        message=\"Time of call is required\",\n        description=\"Verify TimeOfCall is present\",\n    ),\n    ValidationRule(\n        field=\"FullAddress2\",\n        rule_type=ValidationType.REQUIRED,\n        severity=ValidationSeverity.WARNING,\n        message=\"Address is missing\",\n        description=\"Verify address field is present\",\n    ),\n    ValidationRule(\n        field=\"PDZone\",\n        rule_type=ValidationType.REQUIRED,\n        severity=ValidationSeverity.WARNING,\n        message=\"PD Zone is missing\",\n        description=\"Verify PDZone is present\",\n    ),\n    ValidationRule(\n        field=\"Disposition\",\n        rule_type=ValidationType.REQUIRED,\n        severity=ValidationSeverity.WARNING,\n        message=\"Disposition is missing\",\n        description=\"Verify Disposition is present\",\n    ),\n    ValidationRule(\n        field=\"HowReported\",\n        rule_type=ValidationType.REQUIRED,\n        severity=ValidationSeverity.WARNING,\n        message=\"HowReported is missing\",\n        description=\"Verify HowReported is present\",\n    ),\n    \n    # Format validation - Case Number\n    ValidationRule(\n        field=\"ReportNumberNew\",\n        rule_type=ValidationType.REGEX,\n        params={\"pattern\": CASE_NUMBER_PATTERN},\n        severity=ValidationSeverity.ERROR,\n        message=\"Case number format invalid (expected: YY-NNNNNN)\",\n        description=\"Validate case number format\",\n    ),\n    \n    # Domain validation - HowReported\n    ValidationRule(\n        field=\"HowReported\",\n        rule_type=ValidationType.DOMAIN,\n        params={\"values\": HOW_REPORTED_DOMAIN, \"case_sensitive\": False},\n        severity=ValidationSeverity.WARNING,\n        message=\"HowReported value not in valid domain\",\n        description=\"Validate HowReported against allowed values\",\n    ),\n    \n    # Domain validation - Disposition (lenient)\n    ValidationRule(\n        field=\"Disposition\",\n        rule_type=ValidationType.DOMAIN,\n        params={\"values\": DISPOSITION_DOMAIN, \"case_sensitive\": False, \"partial_match\": True},\n        severity=ValidationSeverity.INFO,\n        message=\"Disposition may not be standardized\",\n        description=\"Check Disposition against known values\",\n    ),\n    \n    # Format validation - PDZone (optional)\n    ValidationRule(\n        field=\"PDZone\",\n        rule_type=ValidationType.REGEX,\n        params={\"pattern\": PDZONE_PATTERN, \"allow_null\": True},\n        severity=ValidationSeverity.INFO,\n        message=\"PDZone format unusual\",\n        description=\"Validate PDZone format\",\n    ),\n    \n    # Uniqueness check\n    ValidationRule(\n        field=\"ReportNumberNew\",\n        rule_type=ValidationType.UNIQUE,\n        severity=ValidationSeverity.WARNING,\n        message=\"Duplicate case number detected\",\n        description=\"Check for duplicate case numbers\",\n    ),\n]\n\n\n=============================================================================\n\n\nVectorized Validation Functions\n\n\n=============================================================================\n\n\ndef validate_required_vectorized(series: pd.Series) -> pd.Series:\n    \"\"\"\n    Vectorized required field validation.\n\n\nArgs:\n    series: Pandas Series to validate\n    \nReturns:\n    Boolean Series (True = valid, False = invalid)\n\"\"\"\n# Check for null, empty string, or whitespace-only\nis_valid = series.notna()\n\n# For string columns, also check for empty/whitespace\nif series.dtype == 'object':\n    is_valid = is_valid & (series.astype(str).str.strip() != '')\n    is_valid = is_valid & (series.astype(str).str.lower() != 'nan')\n    is_valid = is_valid & (series.astype(str).str.lower() != 'none')\n\nreturn is_valid\n\n\ndef validate_regex_vectorized(series: pd.Series, pattern: str, allow_null: bool = False) -> pd.Series:\n    \"\"\"\n    Vectorized regex pattern validation.\n\n\nArgs:\n    series: Pandas Series to validate\n    pattern: Regex pattern to match\n    allow_null: Whether null values are considered valid\n    \nReturns:\n    Boolean Series (True = valid, False = invalid)\n\"\"\"\n# Handle nulls\nis_null = series.isna()\n\nif allow_null:\n    # Nulls are valid, check pattern only on non-nulls\n    is_valid = is_null.copy()\n    non_null_mask = ~is_null\n    if non_null_mask.any():\n        is_valid[non_null_mask] = series[non_null_mask].astype(str).str.match(pattern, na=False)\nelse:\n    # Nulls are invalid\n    is_valid = series.astype(str).str.match(pattern, na=False)\n\nreturn is_valid\n\n\ndef validate_domain_vectorized(\n    series: pd.Series,\n    valid_values: Set[str],\n    case_sensitive: bool = False,\n    allow_null: bool = True,\n    partial_match: bool = False,\n) -> pd.Series:\n    \"\"\"\n    Vectorized domain/controlled vocabulary validation.\n\n\nArgs:\n    series: Pandas Series to validate\n    valid_values: Set of allowed values\n    case_sensitive: Whether comparison is case-sensitive\n    allow_null: Whether null values are considered valid\n    partial_match: Allow partial string matches\n    \nReturns:\n    Boolean Series (True = valid, False = invalid)\n\"\"\"\nis_null = series.isna()\n\nif not case_sensitive:\n    valid_upper = {v.upper() for v in valid_values}\n    check_series = series.astype(str).str.upper().str.strip()\nelse:\n    valid_upper = valid_values\n    check_series = series.astype(str).str.strip()\n\nif partial_match:\n    # Check if any valid value is contained in the field\n    def _partial_check(val):\n        if pd.isna(val):\n            return allow_null\n        val_str = str(val).upper() if not case_sensitive else str(val)\n        return any(v in val_str or val_str in v for v in valid_upper)\n    \n    is_valid = series.apply(_partial_check)\nelse:\n    is_valid = check_series.isin(valid_upper)\n\n# Handle nulls\nif allow_null:\n    is_valid = is_valid | is_null\n\nreturn is_valid\n\n\ndef validate_range_vectorized(\n    series: pd.Series,\n    min_val: Optional[float] = None,\n    max_val: Optional[float] = None,\n    allow_null: bool = True,\n) -> pd.Series:\n    \"\"\"\n    Vectorized numeric range validation.\n\n\nArgs:\n    series: Pandas Series to validate\n    min_val: Minimum allowed value (inclusive)\n    max_val: Maximum allowed value (inclusive)\n    allow_null: Whether null values are considered valid\n    \nReturns:\n    Boolean Series (True = valid, False = invalid)\n\"\"\"\n# Convert to numeric\nnumeric = pd.to_numeric(series, errors='coerce')\nis_null = numeric.isna()\n\nis_valid = pd.Series([True] * len(series), index=series.index)\n\nif min_val is not None:\n    is_valid = is_valid & (numeric >= min_val)\nif max_val is not None:\n    is_valid = is_valid & (numeric <= max_val)\n\n# Handle nulls\nif allow_null:\n    is_valid = is_valid | is_null\nelse:\n    is_valid = is_valid & ~is_null\n\nreturn is_valid\n\n\ndef validate_length_vectorized(\n    series: pd.Series,\n    min_length: Optional[int] = None,\n    max_length: Optional[int] = None,\n    allow_null: bool = True,\n) -> pd.Series:\n    \"\"\"\n    Vectorized string length validation.\n\n\nArgs:\n    series: Pandas Series to validate\n    min_length: Minimum string length\n    max_length: Maximum string length\n    allow_null: Whether null values are considered valid\n    \nReturns:\n    Boolean Series (True = valid, False = invalid)\n\"\"\"\nis_null = series.isna()\nlengths = series.astype(str).str.len()\n\nis_valid = pd.Series([True] * len(series), index=series.index)\n\nif min_length is not None:\n    is_valid = is_valid & (lengths >= min_length)\nif max_length is not None:\n    is_valid = is_valid & (lengths <= max_length)\n\n# Handle nulls\nif allow_null:\n    is_valid = is_valid | is_null\nelse:\n    is_valid = is_valid & ~is_null\n\nreturn is_valid\n\n\ndef validate_unique_vectorized(series: pd.Series) -> Tuple[pd.Series, int]:\n    \"\"\"\n    Vectorized uniqueness validation.\n\n\nArgs:\n    series: Pandas Series to validate\n    \nReturns:\n    Tuple of (Boolean Series marking duplicates, duplicate count)\n\"\"\"\n# Mark all occurrences of duplicates (not just subsequent ones)\nduplicated = series.duplicated(keep=False)\n\n# First occurrence is valid, subsequent are invalid\nis_valid = ~series.duplicated(keep='first')\n\n# Null values don't count as duplicates\nis_null = series.isna()\nis_valid = is_valid | is_null\n\ndup_count = duplicated.sum() - series.duplicated(keep='first').sum()\n\nreturn is_valid, dup_count\n\n\ndef validate_date_format_vectorized(\n    series: pd.Series,\n    formats: Optional[List[str]] = None,\n    allow_null: bool = True,\n) -> pd.Series:\n    \"\"\"\n    Vectorized date format validation.\n\n\nArgs:\n    series: Pandas Series to validate\n    formats: List of acceptable date formats\n    allow_null: Whether null values are considered valid\n    \nReturns:\n    Boolean Series (True = valid, False = invalid)\n\"\"\"\nif formats is None:\n    formats = DATE_PATTERNS\n\nis_null = series.isna()\n\n# Try parsing with pandas\ntry:\n    parsed = pd.to_datetime(series, errors='coerce', infer_datetime_format=True)\n    is_valid = parsed.notna()\nexcept Exception:\n    # Fall back to regex matching\n    is_valid = pd.Series([False] * len(series), index=series.index)\n    str_series = series.astype(str)\n    for pattern in formats:\n        is_valid = is_valid | str_series.str.match(pattern, na=False)\n\n# Handle nulls\nif allow_null:\n    is_valid = is_valid | is_null\n\nreturn is_valid\n\n\n=============================================================================\n\n\nValidation Engine Class\n\n\n=============================================================================\n\n\nclass ValidationEngine:\n    \"\"\"\n    Schema-driven validation engine with parallel processing support.\n\n\nProvides comprehensive data validation with:\n- Required field checking\n- Regex pattern compliance\n- Domain/vocabulary enforcement\n- Range and length validation\n- Uniqueness checks\n- Parallel/vectorized processing\n\nAttributes:\n    rules: List of ValidationRule objects\n    schema: Optional JSON schema for field definitions\n    parallel: Whether to use parallel processing\n    max_workers: Number of parallel workers\n\"\"\"\n\ndef __init__(\n    self,\n    rules: Optional[List[ValidationRule]] = None,\n    schema: Optional[Dict] = None,\n    config_path: Optional[Path] = None,\n    parallel: bool = True,\n    max_workers: Optional[int] = None,\n):\n    \"\"\"\n    Initialize ValidationEngine.\n    \n    Args:\n        rules: Custom validation rules (uses CAD defaults if None)\n        schema: JSON schema with field definitions\n        config_path: Path to validation_rules.yaml for additional rules\n        parallel: Enable parallel processing for large datasets\n        max_workers: Number of parallel workers (None = auto)\n    \"\"\"\n    self.schema = schema or {}\n    self.parallel = parallel\n    self.max_workers = max_workers\n    self.rules = rules or build_default_cad_rules()\n    self._results: Optional[ValidationResult] = None\n    \n    # Load additional rules from config\n    if config_path:\n        self._load_rules_from_config(config_path)\n    \n    # Build rules from schema if provided\n    if schema:\n        self._build_rules_from_schema(schema)\n    \n    logger.debug(f\"ValidationEngine initialized with {len(self.rules)} rules\")\n\ndef _load_rules_from_config(self, config_path: Path) -> None:\n    \"\"\"\n    Load additional validation rules from validation_rules.yaml.\n    \n    Args:\n        config_path: Path to validation_rules.yaml\n    \"\"\"\n    try:\n        import yaml\n        \n        with open(config_path, 'r', encoding='utf-8') as f:\n            config = yaml.safe_load(f)\n        \n        # Load required fields\n        required_fields = config.get('required_fields', {})\n        for source_type, fields in required_fields.items():\n            for field_name in fields:\n                # Check if rule already exists\n                exists = any(\n                    r.field == field_name and r.rule_type == ValidationType.REQUIRED\n                    for r in self.rules\n                )\n                if not exists:\n                    self.rules.append(ValidationRule(\n                        field=field_name,\n                        rule_type=ValidationType.REQUIRED,\n                        severity=ValidationSeverity.ERROR,\n                        message=f\"{field_name} is required\",\n                        description=f\"Required field from config ({source_type})\",\n                    ))\n        \n        # Load case number pattern\n        case_config = config.get('case_number', {})\n        if 'pattern' in case_config:\n            # Update existing rule or add new\n            for rule in self.rules:\n                if rule.field == 'ReportNumberNew' and rule.rule_type == ValidationType.REGEX:\n                    rule.params['pattern'] = case_config['pattern']\n                    break\n        \n        # Load domain validation\n        domain_config = config.get('domain_validation', {})\n        for field_name, field_config in domain_config.items():\n            valid_values = set(field_config.get('valid_values', []))\n            if valid_values:\n                # Check for existing domain rule\n                exists = any(\n                    r.field == field_name and r.rule_type == ValidationType.DOMAIN\n                    for r in self.rules\n                )\n                if not exists:\n                    self.rules.append(ValidationRule(\n                        field=field_name,\n                        rule_type=ValidationType.DOMAIN,\n                        params={\n                            \"values\": valid_values,\n                            \"case_sensitive\": False,\n                        },\n                        severity=ValidationSeverity.WARNING,\n                        message=f\"{field_name} value not in valid domain\",\n                        description=f\"Domain validation from config\",\n                    ))\n        \n        logger.info(f\"Loaded validation rules from {config_path}\")\n        \n    except Exception as e:\n        logger.warning(f\"Could not load rules from config: {e}\")\n\ndef _build_rules_from_schema(self, schema: Dict) -> None:\n    \"\"\"\n    Build validation rules from JSON schema.\n    \n    Args:\n        schema: JSON schema with field definitions\n    \"\"\"\n    fields = schema.get('fields', schema.get('properties', {}))\n    \n    for field_name, field_def in fields.items():\n        if isinstance(field_def, dict):\n            # Required check\n            if field_def.get('required', False):\n                exists = any(\n                    r.field == field_name and r.rule_type == ValidationType.REQUIRED\n                    for r in self.rules\n                )\n                if not exists:\n                    self.rules.append(ValidationRule(\n                        field=field_name,\n                        rule_type=ValidationType.REQUIRED,\n                        severity=ValidationSeverity.ERROR,\n                        message=f\"{field_name} is required (from schema)\",\n                    ))\n            \n            # Pattern check\n            if 'pattern' in field_def:\n                exists = any(\n                    r.field == field_name and r.rule_type == ValidationType.REGEX\n                    for r in self.rules\n                )\n                if not exists:\n                    self.rules.append(ValidationRule(\n                        field=field_name,\n                        rule_type=ValidationType.REGEX,\n                        params={\"pattern\": field_def['pattern']},\n                        severity=ValidationSeverity.WARNING,\n                        message=f\"{field_name} format invalid\",\n                    ))\n            \n            # Enum/domain check\n            if 'enum' in field_def:\n                exists = any(\n                    r.field == field_name and r.rule_type == ValidationType.DOMAIN\n                    for r in self.rules\n                )\n                if not exists:\n                    self.rules.append(ValidationRule(\n                        field=field_name,\n                        rule_type=ValidationType.DOMAIN,\n                        params={\"values\": set(field_def['enum'])},\n                        severity=ValidationSeverity.WARNING,\n                        message=f\"{field_name} value not in allowed list\",\n                    ))\n            \n            # Length constraints\n            min_len = field_def.get('minLength')\n            max_len = field_def.get('maxLength')\n            if min_len is not None or max_len is not None:\n                self.rules.append(ValidationRule(\n                    field=field_name,\n                    rule_type=ValidationType.LENGTH,\n                    params={\"min_length\": min_len, \"max_length\": max_len},\n                    severity=ValidationSeverity.WARNING,\n                    message=f\"{field_name} length out of range\",\n                ))\n\ndef add_rule(self, rule: ValidationRule) -> None:\n    \"\"\"\n    Add a validation rule.\n    \n    Args:\n        rule: ValidationRule to add\n    \"\"\"\n    self.rules.append(rule)\n\ndef validate(\n    self,\n    df: pd.DataFrame,\n    schema: Optional[Dict] = None,\n    fields: Optional[List[str]] = None,\n) -> ValidationResult:\n    \"\"\"\n    Validate DataFrame against rules and schema.\n    \n    Args:\n        df: DataFrame to validate\n        schema: Optional schema override\n        fields: Specific fields to validate (None = all)\n        \n    Returns:\n        ValidationResult with comprehensive metrics\n    \"\"\"\n    start_time = datetime.now()\n    \n    if schema:\n        self.schema = schema\n        self._build_rules_from_schema(schema)\n    \n    total_records = len(df)\n    logger.info(f\"Starting validation: {total_records:,} records, {len(self.rules)} rules\")\n    \n    # Initialize tracking\n    field_results: Dict[str, FieldValidationResult] = {}\n    all_errors: List[Dict[str, Any]] = []\n    all_warnings: List[Dict[str, Any]] = []\n    record_validity = pd.Series([True] * total_records, index=df.index)\n    rules_applied = 0\n    \n    # Group rules by field for efficient processing\n    field_rules: Dict[str, List[ValidationRule]] = {}\n    for rule in self.rules:\n        if not rule.enabled:\n            continue\n        if rule.field not in field_rules:\n            field_rules[rule.field] = []\n        field_rules[rule.field].append(rule)\n    \n    # Process each field\n    for field_name, rules in field_rules.items():\n        # Skip if field not in DataFrame\n        if field_name not in df.columns:\n            # Check if any required rules\n            for rule in rules:\n                if rule.rule_type == ValidationType.REQUIRED:\n                    all_errors.append({\n                        \"field\": field_name,\n                        \"rule\": \"required\",\n                        \"message\": f\"Required field '{field_name}' not found in data\",\n                        \"severity\": \"error\",\n                    })\n            continue\n        \n        # Skip if not in requested fields\n        if fields and field_name not in fields:\n            continue\n        \n        # Validate this field\n        result = self._validate_field(df, field_name, rules)\n        field_results[field_name] = result\n        rules_applied += len(result.rules_applied)\n        \n        # Update record validity\n        if result.invalid_count > 0:\n            # Get invalid mask from sample errors\n            for err in result.sample_errors:\n                if 'index' in err:\n                    record_validity.loc[err['index']] = False\n        \n        # Collect errors and warnings\n        for err in result.sample_errors:\n            if err.get('severity') == 'error':\n                all_errors.append(err)\n            else:\n                all_warnings.append(err)\n    \n    # Calculate summary metrics\n    end_time = datetime.now()\n    duration = (end_time - start_time).total_seconds()\n    \n    valid_records = record_validity.sum()\n    invalid_records = total_records - valid_records\n    validation_rate = round(valid_records / total_records * 100, 2) if total_records > 0 else 0\n    \n    self._results = ValidationResult(\n        total_records=total_records,\n        valid_records=int(valid_records),\n        invalid_records=int(invalid_records),\n        error_count=len(all_errors),\n        warning_count=len(all_warnings),\n        fields_validated=len(field_results),\n        rules_applied=rules_applied,\n        validation_rate=validation_rate,\n        duration_seconds=duration,\n        timestamp=start_time.isoformat(),\n        field_results=field_results,\n        errors=all_errors,\n        warnings=all_warnings,\n    )\n    \n    logger.info(\n        f\"Validation complete: {valid_records:,}/{total_records:,} valid \"\n        f\"({validation_rate}%), {len(all_errors)} errors, {len(all_warnings)} warnings\"\n    )\n    \n    return self._results\n\ndef _validate_field(\n    self,\n    df: pd.DataFrame,\n    field_name: str,\n    rules: List[ValidationRule],\n) -> FieldValidationResult:\n    \"\"\"\n    Validate a single field against its rules.\n    \n    Args:\n        df: DataFrame containing the field\n        field_name: Name of field to validate\n        rules: List of rules to apply\n        \n    Returns:\n        FieldValidationResult with metrics\n    \"\"\"\n    series = df[field_name]\n    total = len(series)\n    null_count = int(series.isna().sum())\n    \n    # Track validity across all rules\n    combined_validity = pd.Series([True] * total, index=df.index)\n    rules_applied = []\n    sample_errors = []\n    error_count = 0\n    warning_count = 0\n    \n    for rule in rules:\n        if not rule.enabled:\n            continue\n        \n        rules_applied.append(f\"{rule.rule_type.value}:{rule.field}\")\n        \n        # Apply appropriate validation\n        try:\n            validity = self._apply_rule(series, rule)\n            \n            # Combine with overall validity (AND logic)\n            combined_validity = combined_validity & validity\n            \n            # Collect sample errors\n            invalid_mask = ~validity & series.notna()\n            invalid_count = int(invalid_mask.sum())\n            \n            if invalid_count > 0:\n                if rule.severity == ValidationSeverity.ERROR:\n                    error_count += invalid_count\n                else:\n                    warning_count += invalid_count\n                \n                # Sample some errors for reporting\n                sample_indices = invalid_mask[invalid_mask].head(5).index\n                for idx in sample_indices:\n                    sample_errors.append({\n                        \"field\": field_name,\n                        \"rule\": rule.rule_type.value,\n                        \"index\": idx,\n                        \"value\": str(series.loc[idx])[:100],  # Truncate long values\n                        \"message\": rule.message or f\"Validation failed: {rule.rule_type.value}\",\n                        \"severity\": rule.severity.value,\n                    })\n                    \n        except Exception as e:\n            logger.warning(f\"Rule {rule.rule_type.value} failed on {field_name}: {e}\")\n            sample_errors.append({\n                \"field\": field_name,\n                \"rule\": rule.rule_type.value,\n                \"message\": f\"Rule execution error: {str(e)}\",\n                \"severity\": \"error\",\n            })\n    \n    valid_count = int(combined_validity.sum())\n    invalid_count = total - valid_count\n    \n    return FieldValidationResult(\n        field=field_name,\n        total_records=total,\n        valid_count=valid_count,\n        invalid_count=invalid_count,\n        null_count=null_count,\n        error_count=error_count,\n        warning_count=warning_count,\n        rules_applied=rules_applied,\n        sample_errors=sample_errors,\n    )\n\ndef _apply_rule(self, series: pd.Series, rule: ValidationRule) -> pd.Series:\n    \"\"\"\n    Apply a single validation rule to a series.\n    \n    Args:\n        series: Pandas Series to validate\n        rule: ValidationRule to apply\n        \n    Returns:\n        Boolean Series (True = valid)\n    \"\"\"\n    params = rule.params\n    \n    if rule.rule_type == ValidationType.REQUIRED:\n        return validate_required_vectorized(series)\n    \n    elif rule.rule_type == ValidationType.REGEX:\n        pattern = params.get('pattern', '.