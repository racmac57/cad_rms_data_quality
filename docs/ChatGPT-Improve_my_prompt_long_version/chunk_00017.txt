Let me now provide the script so you can save it at:\n\n\nC:\\Users\\carucci_r\\OneDrive - City of Hackensack\\00_dev\\scripts\\export_raw_csv.py\n\n\n\n📝 export_raw_csv.py\n\n\nThis script will:\n\n\nWalk through _CAD/yearly/ and _CAD/monthly/\n\nLook for .xlsx files missing .csv versions\n\nExport .csv to _CAD/csv/yearly/ or _CAD/csv/monthly/\n\nUse UTF-8 encoding\n\n\nimport os\nimport pandas as pd\nfrom pathlib import Path\n\n# Configuration\nbase_dir = Path(r\"C:\\Users\\carucci_r\\OneDrive - City of Hackensack\\05_EXPORTS\\_CAD\")\nxlsx_dirs = [base_dir / \"yearly\", base_dir / \"monthly\"]\ncsv_dir = base_dir / \"csv\"\ncsv_yearly = csv_dir / \"yearly\"\ncsv_monthly = csv_dir / \"monthly\"\n\n# Ensure output directories exist\ncsv_yearly.mkdir(parents=True, exist_ok=True)\ncsv_monthly.mkdir(parents=True, exist_ok=True)\n\ndef expected_csv_name(xlsx_path: Path, mode: str) -> Path:\n    name = xlsx_path.stem\n    if mode == \"yearly\":\n        return csv_yearly / f\"{name}.csv\"\n    else:\n        return csv_monthly / f\"{name}.csv\"\n\ndef convert_xlsx_to_csv(xlsx_path: Path, output_path: Path):\n    try:\n        df = pd.read_excel(xlsx_path, engine=\"openpyxl\")\n        df.to_csv(output_path, index=False, encoding=\"utf-8\")\n        print(f\"[✓] Exported: {output_path.name}\")\n    except Exception as e:\n        print(f\"[✗] Failed: {xlsx_path.name} — {e}\")\n\n# Main loop\nfor mode, xlsx_root in zip([\"yearly\", \"monthly\"], xlsx_dirs):\n    for root, _, files in os.walk(xlsx_root):\n        for file in files:\n            if file.endswith(\".xlsx\") and not file.startswith(\"~$\"):\n                xlsx_path = Path(root) / file\n                csv_path = expected_csv_name(xlsx_path, mode)\n                if not csv_path.exists():\n                    convert_xlsx_to_csv(xlsx_path, csv_path)\n                else:\n                    print(f\"[=] Skipped (exists): {csv_path.name}\")\n\n\n\n✅ 3. After Saving the Script\n\n\nOnce saved to:\n\n\nC:\\Users\\carucci_r\\OneDrive - City of Hackensack\\00_dev\\scripts\\export_raw_csv.py\n\n\nRun it with:\n\n\npython \"C:\\Users\\carucci_r\\OneDrive - City of Hackensack\\00_dev\\scripts\\export_raw_csv.py\"\n\n\n\n🔄 Let Me Know If You’d Like:\n\n\nAuto-generate Kanban task cards for any missing .csv\n\nLogging output to a .txt or .csv\n\nOr integrate this into your existing Obsidian task_templates/\n\n\nReady to assist with the next step!" }
  ]
}