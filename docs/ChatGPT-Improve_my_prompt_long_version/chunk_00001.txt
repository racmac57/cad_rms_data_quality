Defaults to project's config/ folder.\n    \"\"\"\n    if config_dir is None:\n        # Navigate from shared/utils/ up to project root, then to config/\n        self.config_dir = Path(__file__).parents[2] / \"config\"\n    else:\n        self.config_dir = Path(config_dir)\n    \n    self._cache: Dict[str, Dict] = {}\n    self._schemas_config: Optional[Dict] = None\n    \n    logger.debug(f\"ConfigLoader initialized with config_dir: {self.config_dir}\")\n\ndef load_yaml(self, filename: str, use_cache: bool = True) -> Dict[str, Any]:\n    \"\"\"\n    Load YAML config file with variable expansion.\n    \n    Args:\n        filename: Name of YAML file (e.g., 'schemas.yaml')\n        use_cache: Whether to use cached result if available\n        \n    Returns:\n        Parsed and expanded configuration dictionary\n        \n    Raises:\n        FileNotFoundError: If config file doesn't exist\n        yaml.YAMLError: If YAML parsing fails\n    \"\"\"\n    cache_key = f\"yaml:{filename}\"\n    if use_cache and cache_key in self._cache:\n        logger.debug(f\"Returning cached config: {filename}\")\n        return self._cache[cache_key]\n    \n    filepath = self.config_dir / filename\n    if not filepath.exists():\n        raise FileNotFoundError(f\"Configuration file not found: {filepath}\")\n    \n    logger.info(f\"Loading config: {filepath}\")\n    \n    with open(filepath, \"r\", encoding=\"utf-8\") as f:\n        raw_config = yaml.safe_load(f)\n    \n    if raw_config is None:\n        raw_config = {}\n    \n    # Build context from top-level string values for variable expansion\n    context = {k: v for k, v in raw_config.items() if isinstance(v, str)}\n    expanded = self._expand_variables(raw_config, context)\n    \n    if use_cache:\n        self._cache[cache_key] = expanded\n    \n    return expanded\n\ndef _expand_variables(self, obj: Any, context: Dict[str, str]) -> Any:\n    \"\"\"\n    Recursively expand ${variable} references in configuration.\n    \n    Args:\n        obj: Object to expand (str, dict, list, or other)\n        context: Dictionary of variable names to values\n        \n    Returns:\n        Object with all ${var} references expanded\n    \"\"\"\n    if isinstance(obj, str):\n        # Find all ${variable} patterns\n        pattern = r\"\\$\\{(\\w+)\\}\"\n        \n        def replacer(match):\n            var_name = match.group(1)\n            if var_name in context:\n                return str(context[var_name])\n            logger.warning(f\"Undefined variable in config: ${{{var_name}}}\")\n            return match.group(0)  # Return original if not found\n        \n        return re.sub(pattern, replacer, obj)\n    \n    elif isinstance(obj, dict):\n        return {k: self._expand_variables(v, context) for k, v in obj.items()}\n    \n    elif isinstance(obj, list):\n        return [self._expand_variables(item, context) for item in obj]\n    \n    return obj\n\ndef load_schema(self, schema_key: str) -> Dict[str, Any]:\n    \"\"\"\n    Load JSON schema from 09_Reference/Standards.\n    \n    Args:\n        schema_key: Key from schemas.yaml (e.g., 'canonical', 'cad', 'rms')\n        \n    Returns:\n        Parsed JSON schema dictionary\n        \n    Raises:\n        KeyError: If schema_key not found in schemas.yaml\n        FileNotFoundError: If schema file doesn't exist\n        json.JSONDecodeError: If JSON parsing fails\n    \"\"\"\n    if self._schemas_config is None:\n        self._schemas_config = self.load_yaml(\"schemas.yaml\")\n    \n    schemas = self._schemas_config.get(\"schemas\", {})\n    if schema_key not in schemas:\n        available = list(schemas.keys())\n        raise KeyError(\n            f\"Schema key '{schema_key}' not found. Available: {available}\"\n        )\n    \n    schema_path = Path(schemas[schema_key])\n    \n    if not schema_path.exists():\n        raise FileNotFoundError(f\"Schema file not found: {schema_path}\")\n    \n    logger.info(f\"Loading schema '{schema_key}': {schema_path}\")\n    \n    with open(schema_path, \"r\", encoding=\"utf-8\") as f:\n        return json.load(f)\n\ndef load_mapping(self, mapping_key: str) -> Union[Dict[str, Any], List[Path]]:\n    \"\"\"\n    Load mapping file(s) from 09_Reference/Standards.\n    \n    Supports glob patterns (e.g., 'call_types_*.csv').\n    \n    Args:\n        mapping_key: Key from schemas.yaml mappings section\n        \n    Returns:\n        For JSON: Parsed dictionary\n        For glob patterns: List of matching file paths\n        For other files: File path\n        \n    Raises:\n        KeyError: If mapping_key not found\n        FileNotFoundError: If file(s) not found\n    \"\"\"\n    if self._schemas_config is None:\n        self._schemas_config = self.load_yaml(\"schemas.yaml\")\n    \n    mappings = self._schemas_config.get(\"mappings\", {})\n    if mapping_key not in mappings:\n        available = list(mappings.keys())\n        raise KeyError(\n            f\"Mapping key '{mapping_key}' not found. Available: {available}\"\n        )\n    \n    mapping_path = mappings[mapping_key]\n    \n    # Handle glob patterns\n    if \"*\" in mapping_path:\n        matched_files = sorted(glob(mapping_path))\n        if not matched_files:\n            raise FileNotFoundError(\n                f\"No files matched pattern: {mapping_path}\"\n            )\n        logger.info(f\"Glob pattern matched {len(matched_files)} files\")\n        return [Path(f) for f in matched_files]\n    \n    path = Path(mapping_path)\n    \n    if not path.exists():\n        raise FileNotFoundError(f\"Mapping file not found: {path}\")\n    \n    # Load JSON mappings directly\n    if path.suffix == \".json\":\n        logger.info(f\"Loading mapping '{mapping_key}': {path}\")\n        with open(path, \"r\", encoding=\"utf-8\") as f:\n            return json.load(f)\n    \n    # Return path for non-JSON files (markdown, csv, etc. )\n    return path\n\ndef load_config_file(self, config_key: str) -> Union[Dict[str, Any], Path]:\n    \"\"\"\n    Load runtime configuration file from config section.\n    \n    Args:\n        config_key: Key from schemas.yaml config section\n        \n    Returns:\n        For JSON: Parsed dictionary\n        For other files: File path\n    \"\"\"\n    if self._schemas_config is None:\n        self._schemas_config = self.load_yaml(\"schemas.yaml\")\n    \n    configs = self._schemas_config.get(\"config\", {})\n    if config_key not in configs:\n        available = list(configs.keys())\n        raise KeyError(\n            f\"Config key '{config_key}' not found. Available: {available}\"\n        )\n    \n    config_path = Path(configs[config_key])\n    \n    if not config_path.exists():\n        raise FileNotFoundError(f\"Config file not found: {config_path}\")\n    \n    if config_path.suffix == \".json\":\n        logger.info(f\"Loading config '{config_key}': {config_path}\")\n        with open(config_path, \"r\", encoding=\"utf-8\") as f:\n            return json.load(f)\n    \n    return config_path\n\ndef validate_all_paths(self) -> Dict[str, bool]:\n    \"\"\"\n    Verify all referenced schema, mapping, and config files exist.\n    \n    Returns:\n        Dictionary mapping path identifiers to existence status (True/False)\n    \"\"\"\n    if self._schemas_config is None:\n        self._schemas_config = self.load_yaml(\"schemas.yaml\")\n    \n    results = {}\n    \n    for section in [\"schemas\", \"mappings\", \"config\"]:\n        section_data = self._schemas_config.get(section, {})\n        \n        for key, path_str in section_data.items():\n            full_key = f\"{section}. {key}\"\n            \n            # Handle glob patterns\n            if \"*\" in str(path_str):\n                matched = glob(path_str)\n                results[full_key] = len(matched) > 0\n                if matched:\n                    logger.debug(f\"✓ {full_key}: {len(matched)} files matched\")\n                else:\n                    logger.warning(f\"✗ {full_key}: No files matched pattern\")\n            else:\n                exists = Path(path_str).exists()\n                results[full_key] = exists\n                if exists:\n                    logger.debug(f\"✓ {full_key}: Found\")\n                else:\n                    logger.warning(f\"✗ {full_key}: NOT FOUND - {path_str}\")\n    \n    return results\n\ndef get_validation_summary(self) -> str:\n    \"\"\"\n    Generate human-readable validation summary.\n    \n    Returns:\n        Formatted string with path validation results\n    \"\"\"\n    results = self.validate_all_paths()\n    \n    lines = [\"Schema/Mapping Path Validation Summary\", \"=\" * 45]\n    \n    found = sum(1 for v in results.values() if v)\n    total = len(results)\n    \n    lines.append(f\"Found: {found}/{total} paths\\n\")\n    \n    for key, exists in sorted(results.items()):\n        status = \"✓\" if exists else \"✗ MISSING\"\n        lines.append(f\"  {status} {key}\")\n    \n    return \"\\n\".join(lines)\n\ndef clear_cache(self):\n    \"\"\"Clear all cached configurations.\"\"\"\n    self._cache.clear()\n    self._schemas_config = None\n    logger.debug(\"Configuration cache cleared\")\n\n\nModule-level singleton and convenience functions\n\n\n_loader: Optional[ConfigLoader] = None\n\n\ndef get_loader(config_dir: Optional[Path] = None) -> ConfigLoader:\n    \"\"\"\n    Get or create the singleton ConfigLoader instance.\n\n\nArgs:\n    config_dir: Optional custom config directory\n    \nReturns:\n    ConfigLoader instance\n\"\"\"\nglobal _loader\nif _loader is None or config_dir is not None:\n    _loader = ConfigLoader(config_dir)\nreturn _loader\n\n\ndef load_config(name: str) -> Dict[str, Any]:\n    \"\"\"\n    Convenience function to load a YAML config file.\n\n\nArgs:\n    name: Config filename (e.g., 'validation_rules.yaml')\n    \nReturns:\n    Parsed configuration dictionary\n\"\"\"\nreturn get_loader().load_yaml(name)\n\n\ndef load_schema(key: str) -> Dict[str, Any]:\n    \"\"\"\n    Convenience function to load a JSON schema.\n\n\nArgs:\n    key: Schema key from schemas.yaml (e.g., 'cad', 'rms', 'canonical')\n    \nReturns:\n    Parsed JSON schema dictionary\n\"\"\"\nreturn get_loader().load_schema(key)\n\n\ndef validate_paths() -> Dict[str, bool]:\n    \"\"\"\n    Convenience function to validate all configured paths.\n\n\nReturns:\n    Dictionary mapping path identifiers to existence status\n\"\"\"\nreturn get_loader().validate_all_paths()\n\n\ndef print_validation_summary():\n    \"\"\"Print formatted validation summary to stdout.\"\"\"\n    print(get_loader().get_validation_summary())\n\n\nCLI support for standalone validation\n\n\nif name == \"main\":\n    logging.basicConfig(\n        level=logging.INFO,\n        format=\"%(asctime)s - %(levelname)s - %(message)s\"\n    )\n    print_validation_summary()\n\n\n\n3a. requirements.txt\n\n\ntxt\n\n\n// 2026-01-30-10-34-00 EST\n\n\n// cad_rms_data_quality/requirements.txt\n\n\n// Author: R. A. Carucci\n\n\n// Purpose: Python package dependencies for CAD/RMS data quality system\n\n\nCore data processing\n\n\npandas>=2.0.0\nnumpy>=1.24.0\n\n\nConfiguration\n\n\npyyaml>=6.0\n\n\nExcel file support\n\n\nopenpyxl>=3.1.0\nxlrd>=2.0.1\n\n\nAddress parsing and validation\n\n\nusaddress>=0.5.10\n\n\nFuzzy string matching\n\n\nrapidfuzz>=3.0.0\n\n\nHTML report generation\n\n\njinja2>=3.1.0\n\n\nProgress bars (optional)\n\n\ntqdm>=4.65.0\n\n\nLogging enhancements (optional)\n\n\ncolorlog>=6.7.0\n\n\nTesting\n\n\npytest>=7.4.0\npytest-cov>=4.1.0\n\n\nCode quality (dev)\n\n\nruff>=0.1.0\nmypy>=1.5.0\n\n\nType stubs for mypy\n\n\npandas-stubs>=2.0.0\ntypes-PyYAML>=6.0.0\n\n\n\n3b. pyproject.toml\n\n\ntoml\n\n\n// 2026-01-30-10-35-00 EST\n\n\n// cad_rms_data_quality/pyproject.toml\n\n\n// Author: R. A. Carucci\n\n\n// Purpose: Project configuration and metadata for CAD/RMS data quality system\n\n\n[project]\nname = \"cad-rms-data-quality\"\nversion = \"1.0.0\"\ndescription = \"Unified CAD/RMS data quality system for validation and consolidation\"\nreadme = \"README.md\"\nlicense = {text = \"Internal Use - City of Hackensack Police Department\"}\nauthors = [\n    {name = \"R. A. Carucci\", email = \"carucci_r@hackensack.org\"}\n]\nrequires-python = \">=3.9\"\nkeywords = [\"cad\", \"rms\", \"data-quality\", \"validation\", \"police\", \"law-enforcement\"]\nclassifiers = [\n    \"Development Status :: 4 - Beta\",\n    \"Intended Audience :: Developers\",\n    \"Programming Language :: Python :: 3\",\n    \"Programming Language :: Python :: 3.9\",\n    \"Programming Language :: Python :: 3.10\",\n    \"Programming Language :: Python :: 3.11\",\n    \"Programming Language :: Python :: 3.12\",\n    \"Topic :: Scientific/Engineering :: Information Analysis\",\n]\n\n\ndependencies = [\n    \"pandas>=2.0.0\",\n    \"numpy>=1.24.0\",\n    \"pyyaml>=6.0\",\n    \"openpyxl>=3.1.0\",\n    \"usaddress>=0.5.10\",\n    \"rapidfuzz>=3.0.0\",\n    \"jinja2>=3.1.0\",\n    \"tqdm>=4.65.0\",\n]\n\n\n[project.optional-dependencies]\ndev = [\n    \"pytest>=7.4.0\",\n    \"pytest-cov>=4.1.0\",\n    \"ruff>=0.1.0\",\n    \"mypy>=1.5.0\",\n    \"pandas-stubs>=2.0.0\",\n    \"types-PyYAML>=6.0.0\",\n]\n\n\n[project.scripts]\nconsolidate-cad = \"consolidation.scripts.consolidate_cad:main\"\nvalidate-cad = \"monthly_validation.scripts.validate_cad:main\"\nvalidate-rms = \"monthly_validation.scripts.validate_rms:main\"\ncheck-schemas = \"shared.utils.schema_loader:print_validation_summary\"\n\n\n[project.urls]\nDocumentation = \"docs/\"\nRepository = \"https://github.com/hackensack-pd/cad_rms_data_quality\"\n\n\n[build-system]\nrequires = [\"setuptools>=61.0\", \"wheel\"]\nbuild-backend = \"setuptools.build_meta\"\n\n\n[tool.setuptools.packages.find]\nwhere = [\".\"]\ninclude = [\"shared*\", \"consolidation*\", \"monthly_validation*\"]\n\n\n[tool.pytest.ini_options]\ntestpaths = [\"tests\"]\npython_files = [\"test_*.py\"]\npython_functions = [\"test_*\"]\naddopts = \"-v --tb=short\"\nfilterwarnings = [\n    \"ignore::DeprecationWarning\",\n]\n\n\n[tool.ruff]\nline-length = 100\ntarget-version = \"py39\"\nselect = [\n    \"E\",    # pycodestyle errors\n    \"W\",    # pycodestyle warnings\n    \"F\",    # pyflakes\n    \"I\",    # isort\n    \"B\",    # flake8-bugbear\n    \"C4\",   # flake8-comprehensions\n    \"UP\",   # pyupgrade\n]\nignore = [\n    \"E501\",  # line too long (handled by formatter)\n]\n\n\n[tool.ruff.isort]\nknown-first-party = [\"shared\", \"consolidation\", \"monthly_validation\"]\n\n\n[tool.mypy]\npython_version = \"3.9\"\nwarn_return_any = true\nwarn_unused_configs = true\nignore_missing_imports = true\n\n\n\n3c. .gitignore\n\n\ngitignore\n\n\n// 2026-01-30-10-36-00 EST\n\n\n// cad_rms_data_quality/.gitignore\n\n\n// Author: R. A. Carucci\n\n\n// Purpose: Git ignore rules for CAD/RMS data quality project\n\n\n=============================================================================\n\n\nPython\n\n\n=============================================================================\n\n\npycache/\n*.py[cod]\n*$py.class\n*.so\n.Python\nbuild/\ndevelop-eggs/\ndist/\ndownloads/\neggs/\n.eggs/\nlib/\nlib64/\nparts/\nsdist/\nvar/\nwheels/\nshare/python-wheels/\n*.egg-info/\n.installed.cfg\n*.egg\nMANIFEST\n\n\n=============================================================================\n\n\nVirtual Environments\n\n\n=============================================================================\n\n\nenv/\nvenv/\nENV/\nenv.bak/\nvenv.bak/\n.venv/\n.conda/\n\n\n=============================================================================\n\n\nIDE and Editors\n\n\n=============================================================================\n\n\n.vscode/\n.idea/\n*.swp\n*.swo\n*.swn\n*~\n.project\n.pydevproject\n.spyderproject\n.spyproject\n*.sublime-workspace\n*.sublime-project\n\n\n=============================================================================\n\n\nTesting\n\n\n=============================================================================\n\n\n.pytest_cache/\n.coverage\n.coverage. *\nhtmlcov/\n.tox/\n.nox/\ncoverage.xml\n*.cover\n*.py,cover\n.hypothesis/\n\n\n=============================================================================\n\n\nType Checking\n\n\n=============================================================================\n\n\n.mypy_cache/\n.dmypy.json\ndmypy.json\n\n\n=============================================================================\n\n\nData Outputs (Large Files)\n\n\n=============================================================================\n\n\nconsolidation/output/.csv\nconsolidation/output/.xlsx\nconsolidation/reports/.html\nconsolidation/reports/.xlsx\nconsolidation/reports/.json\nconsolidation/logs/.log\n\n\nmonthly_validation/reports//\nmonthly_validation/logs/.log\n\n\n=============================================================================\n\n\nLogs\n\n\n=============================================================================\n\n\n*.log\nlogs/\n\n\n=============================================================================\n\n\nOS Generated\n\n\n=============================================================================\n\n\n.DS_Store\n.DS_Store?\n._*\n.Spotlight-V100\n.Trashes\nehthumbs.db\nThumbs.db\ndesktop.ini\n\n\n=============================================================================\n\n\nJupyter Notebooks\n\n\n=============================================================================\n\n\n.ipynb_checkpoints/\n*.ipynb\n\n\n=============================================================================\n\n\nSecrets and Credentials\n\n\n=============================================================================\n\n\n.env\n.env.local\n.env. *.local\n*.pem\n*.key\nsecrets.yaml\ncredentials.json\n\n\n=============================================================================\n\n\nTemporary Files\n\n\n=============================================================================\n\n\n*.tmp\n*.temp\n*.bak\n.backup\n~$\n\n\n=============================================================================\n\n\nKeep Directory Structure (override above ignores)\n\n\n=============================================================================\n\n\n!consolidation/output/.gitkeep\n!consolidation/reports/.gitkeep\n!consolidation/logs/.gitkeep\n!monthly_validation/reports/.gitkeep\n!monthly_validation/logs/.gitkeep\n!tests/fixtures/.gitkeep\n\n\n\n3d. Makefile\n\n\nmakefile\n\n\n// 2026-01-30-10-37-00 EST\n\n\n// cad_rms_data_quality/Makefile\n\n\n// Author: R. A. Carucci\n\n\n// Purpose: Automation commands for CAD/RMS data quality system\n\n\n.PHONY: help setup test lint typecheck consolidate validate-cad validate-rms clean check-schemas\n\n\nDefault target\n\n\nhelp:\n\t@echo \"==============================================\"\n\t@echo \"CAD/RMS Data Quality System - Make Targets\"\n\t@echo \"==============================================\"\n\t@echo \"\"\n\t@echo \"Setup & Development:\"\n\t@echo \"  make setup          Install all dependencies\"\n\t@echo \"  make setup-dev      Install with dev dependencies\"\n\t@echo \"  make test           Run pytest suite\"\n\t@echo \"  make lint           Run ruff linter\"\n\t@echo \"  make typecheck      Run mypy type checker\"\n\t@echo \"  make check-schemas  Validate schema paths exist\"\n\t@echo \"\"\n\t@echo \"Data Processing:\"\n\t@echo \"  make consolidate    Run 2019-2026 CAD consolidation\"\n\t@echo \"  make validate-cad   Validate monthly CAD (requires INPUT=path)\"\n\t@echo \"  make validate-rms   Validate monthly RMS (requires INPUT=path)\"\n\t@echo \"\"\n\t@echo \"Maintenance:\"\n\t@echo \"  make clean          Remove generated files and caches\"\n\t@echo \"\"\n\t@echo \"Examples:\"\n\t@echo \"  make validate-cad INPUT=\"path/to/2026_02_CAD.xlsx\"\"\n\t@echo \"\"\n\n\n=============================================================================\n\n\nSetup\n\n\n=============================================================================\n\n\nsetup:\n\tpip install -r requirements.txt\n\t@echo \"\"\n\t@echo \"✓ Dependencies installed. Run 'make check-schemas' to validate paths.\"\n\n\nsetup-dev:\n\tpip install -r requirements.txt\n\tpip install pytest pytest-cov ruff mypy pandas-stubs types-PyYAML\n\t@echo \"\"\n\t@echo \"✓ Dev dependencies installed.\"\n\n\n=============================================================================\n\n\nCode Quality\n\n\n=============================================================================\n\n\ntest:\n\tpytest tests/ -v --cov=shared --cov-report=term-missing --cov-report=html\n\t@echo \"\"\n\t@echo \"✓ Tests complete. Coverage report: htmlcov/index.html\"\n\n\nlint:\n\truff check shared consolidation monthly_validation --fix\n\t@echo \"\"\n\t@echo \"✓ Linting complete.\"\n\n\ntypecheck:\n\tmypy shared --ignore-missing-imports\n\t@echo \"\"\n\t@echo \"✓ Type checking complete.\"\n\n\ncheck-schemas:\n\tpython -m shared.utils.schema_loader\n\t@echo \"\"\n\n\n=============================================================================\n\n\nData Processing\n\n\n=============================================================================\n\n\nconsolidate:\n\t@echo \"Starting 2019-2026 CAD consolidation...\"\n\tpython consolidation/scripts/consolidate_cad.py\n\t@echo \"\"\n\t@echo \"Preparing ArcGIS-ready dataset...\"\n\tpython consolidation/scripts/prepare_arcgis.py\n\t@echo \"\"\n\t@echo \"✓ Consolidation complete. Check consolidation/output/\"\n\n\nvalidate-cad:\nifndef INPUT\n\t@echo \"ERROR: INPUT parameter required\"\n\t@echo \"Usage: make validate-cad INPUT=\"path/to/cad_export.xlsx\"\"\n\t@exit 1\nendif\n\t@echo \"Validating CAD export: $(INPUT)\"\n\tpython monthly_validation/scripts/validate_cad.py --input \"$(INPUT)\"\n\t@echo \"\"\n\t@echo \"✓ Validation complete. Check monthly_validation/reports/\"\n\n\nvalidate-rms:\nifndef INPUT\n\t@echo \"ERROR: INPUT parameter required\"\n\t@echo \"Usage: make validate-rms INPUT=\"path/to/rms_export.xlsx\"\"\n\t@exit 1\nendif\n\t@echo \"Validating RMS export: $(INPUT)\"\n\tpython monthly_validation/scripts/validate_rms.py --input \"$(INPUT)\"\n\t@echo \"\"\n\t@echo \"✓ Validation complete. Check monthly_validation/reports/\"\n\n\n=============================================================================\n\n\nMaintenance\n\n\n=============================================================================\n\n\nclean:\n\t@echo \"Cleaning generated files...\"\n\tfind . -type d -name \"pycache\" -exec rm -rf {} + 2>/dev/null || true\n\tfind . -type d -name \".pytest_cache\" -exec rm -rf {} + 2>/dev/null || true\n\tfind . -type d -name \".mypy_cache\" -exec rm -rf {} + 2>/dev/null || true\n\tfind . -type d -name \"htmlcov\" -exec rm -rf {} + 2>/dev/null || true\n\tfind . -type f -name \"*.pyc\" -delete 2>/dev/null || true\n\tfind . -type f -name \".coverage\" -delete 2>/dev/null || true\n\t@echo \"✓ Clean complete.\"\n\n\n=============================================================================\n\n\nDirectory Setup (run once)\n\n\n=============================================================================\n\n\ninit-dirs:\n\t@mkdir -p consolidation/output\n\t@mkdir -p consolidation/reports\n\t@mkdir -p consolidation/logs\n\t@mkdir -p monthly_validation/reports\n\t@mkdir -p monthly_validation/logs\n\t@mkdir -p tests/fixtures\n\t@touch consolidation/output/.gitkeep\n\t@touch consolidation/reports/.gitkeep\n\t@touch consolidation/logs/.gitkeep\n\t@touch monthly_validation/reports/.gitkeep\n\t@touch monthly_validation/logs/.gitkeep\n\t@touch tests/fixtures/.gitkeep\n\t@echo \"✓ Directory structure initialized.\"\n\n\n\n4. consolidation/scripts/consolidate_cad.py\n\n\npython\n\n\n// 2026-01-30-10-38-00 EST\n\n\n// cad_rms_data_quality/consolidation/scripts/consolidate_cad.py\n\n\n// Author: R. A. Carucci\n\n\n// Purpose: Consolidate 2019-2026 CAD exports into single validated dataset\n\n\n\"\"\"\nCAD Historical Consolidation Script\n\n\nMerges 8 annual CAD export files (2019-2026) into a single validated dataset\nfor ArcGIS Pro dashboards.