No files processed.\")\n        return None\n    \n    # -------------------------------------------------------------------------\n    # Step 2: Load schema\n    # -------------------------------------------------------------------------\n    logger.info(\"\\n[Step 2/7] Loading CAD schema...\")\n    \n    try:\n        cad_schema = load_schema(\"cad\")\n        logger.info(f\"Schema loaded with {len(cad_schema.get('fields', []))} field definitions\")\n    except (KeyError, FileNotFoundError) as e:\n        logger.warning(f\"Could not load CAD schema: {e}\")\n        logger.warning(\"Proceeding without schema validation\")\n        cad_schema = {}\n    \n    # -------------------------------------------------------------------------\n    # Step 3: Load source files\n    # -------------------------------------------------------------------------\n    logger.info(\"\\n[Step 3/7] Loading source files...\")\n    \n    dataframes: List[pd.DataFrame] = []\n    file_hashes: Dict[int, str] = {}\n    \n    for source in tqdm(sources, desc=\"Loading files\"):\n        df = load_source_file(source)\n        if df is not None:\n            dataframes.append(df)\n            # Compute hash for audit trail\n            filepath = Path(source[\"path\"])\n            if filepath.exists():\n                file_hashes[source[\"year\"]] = compute_file_hash(filepath)\n    \n    if not dataframes:\n        logger.error(\"No source files loaded successfully\")\n        return None\n    \n    logger.info(f\"Successfully loaded {len(dataframes)} files\")\n    \n    # -------------------------------------------------------------------------\n    # Step 4: Concatenate all years\n    # -------------------------------------------------------------------------\n    logger.info(\"\\n[Step 4/7] Concatenating datasets...\")\n    \n    df_combined = pd.concat(dataframes, ignore_index=True)\n    logger.info(f\"Combined dataset: {len(df_combined):,} records\")\n    \n    # -------------------------------------------------------------------------\n    # Step 5: Normalize and validate\n    # -------------------------------------------------------------------------\n    logger.info(\"\\n[Step 5/7] Normalizing fields...\")\n    \n    df_normalized = normalize_fields(df_combined, cad_schema)\n    \n    logger.info(\"\\n[Step 6/7] Validating records...\")\n    \n    df_validated, validation_results = validate_dataframe(df_normalized, cad_schema)\n    \n    quality_score = calculate_quality_score(df_validated, validation_results)\n    logger.info(f\"Quality score: {quality_score}/100\")\n    \n    # -------------------------------------------------------------------------\n    # Step 6: Deduplicate\n    # -------------------------------------------------------------------------\n    logger.info(\"\\n[Step 7/7] Deduplicating records...\")\n    \n    dedup_key = sources_config.get(\"processing\", {}).get(\"dedup_field\", \"ReportNumberNew\")\n    df_final = deduplicate_records(df_validated, dedup_key)\n    \n    # Detect gaps\n    gap_df = detect_gaps(df_final)\n    \n    # -------------------------------------------------------------------------\n    # Metrics summary\n    # -------------------------------------------------------------------------\n    end_time = datetime.now()\n    duration = (end_time - start_time).total_seconds()\n    \n    metrics = {\n        \"run_timestamp\": start_time.isoformat(),\n        \"duration_seconds\": duration,\n        \"source_files\": len(sources),\n        \"files_loaded\": len(dataframes),\n        \"records_combined\": len(df_combined),\n        \"records_final\": len(df_final),\n        \"duplicates_removed\": len(df_combined) - len(df_final),\n        \"quality_score\": quality_score,\n        \"validation_errors\": validation_results.get(\"error_count\", 0),\n        \"gaps_detected\": len(gap_df),\n        \"file_hashes\": file_hashes,\n        \"years_covered\": sorted([s[\"year\"] for s in sources]),\n    }\n    \n    logger.info(\"\\n\" + \"=\" * 60)\n    logger.info(\"CONSOLIDATION SUMMARY\")\n    logger.info(\"=\" * 60)\n    logger.info(f\"  Source files:      {metrics['files_loaded']}/{metrics['source_files']}\")\n    logger.info(f\"  Records combined:  {metrics['records_combined']:,}\")\n    logger.info(f\"  Duplicates removed:{metrics['duplicates_removed']:,}\")\n    logger.info(f\"  Final records:     {metrics['records_final']:,}\")\n    logger.info(f\"  Quality score:     {metrics['quality_score']}/100\")\n    logger.info(f\"  Gaps detected:     {metrics['gaps_detected']}\")\n    logger.info(f\"  Duration:          {duration:.1f} seconds\")\n    logger.info(\"=\" * 60)\n    \n    if validate_only:\n        logger.info(\"\\n[VALIDATE ONLY] Skipping file output.\")\n        return df_final\n    \n    # -------------------------------------------------------------------------\n    # Save outputs\n    # -------------------------------------------------------------------------\n    output_config = sources_config.get(\"output\", {})\n    output_dir = PROJECT_ROOT / output_config.get(\"directory\", \"consolidation/output\")\n    output_dir.mkdir(parents=True, exist_ok=True)\n    \n    output_file = output_dir / output_config.get(\"consolidated_file\", \"2019_2026_CAD_Consolidated.csv\")\n    \n    logger.info(f\"\\nSaving consolidated dataset: {output_file}\")\n    \n    # Remove internal tracking columns before export\n    export_cols = [c for c in df_final.columns if not c.startswith(\"_\")]\n    df_final[export_cols].to_csv(\n        output_file,\n        index=False,\n        encoding=output_config.get(\"encoding\", \"utf-8\"),\n    )\n    \n    logger.info(f\"✓ Saved {len(df_final):,} records to {output_file.name}\")\n    \n    # Generate reports\n    report_dir = PROJECT_ROOT / sources_config.get(\"reports\", {}).get(\"directory\", \"consolidation/reports\")\n    generate_consolidation_report(metrics, report_dir)\n    \n    # Save gap analysis if any gaps found\n    if len(gap_df) > 0:\n        gap_file = report_dir / \"gap_analysis.xlsx\"\n        gap_df.to_excel(gap_file, index=False)\n        logger.info(f\"✓ Gap analysis saved: {gap_file.name}\")\n    \n    logger.info(\"\\n✓ Consolidation complete!\")\n    \n    return df_final\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    parser = argparse.ArgumentParser(\n        description=\"Consolidate 2019-2026 CAD exports into single validated dataset\"\n    )\n    parser.add_argument(\n        \"--dry-run\",\n        action=\"store_true\",\n        help=\"Validate inputs without processing\",\n    )\n    parser.add_argument(\n        \"--validate-only\",\n        action=\"store_true\",\n        help=\"Process and validate but don't save outputs\",\n    )\n    parser.add_argument(\n        \"--verbose\", \"-v\",\n        action=\"store_true\",\n        help=\"Enable debug logging\",\n    )\n    \n    args = parser.parse_args()\n    \n    if args.verbose:\n        logging.getLogger().setLevel(logging.DEBUG)\n    \n    result = consolidate(\n        dry_run=args.dry_run,\n        validate_only=args.validate_only,\n    )\n    \n    if result is None and not args.dry_run:\n        sys.exit(1)\n    \n    sys.exit(0)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n\nSummary Checklist\n\nFile\nStatus\nconfig/schemas.yaml\n✅ Ready\nconfig/validation_rules.yaml\n✅ Ready\nconfig/consolidation_sources.yaml\n✅ Ready\nshared/utils/schema_loader.py\n✅ Ready\nrequirements.txt\n✅ Ready\npyproject.toml\n✅ Ready\n.gitignore\n✅ Ready\nMakefile\n✅ Ready\nconsolidation/scripts/consolidate_cad.py\n✅ Ready (with stubs)\n\nNext steps after creating these files:\n\n\nRun make init-dirs to create output directories with .gitkeep\n\nRun make setup to install dependencies\n\nRun make check-schemas to validate 09_Reference/Standards paths\n\nRun python consolidation/scripts/consolidate_cad.py --dry-run to verify source files"
    },
    {
      "role": "Prompt",
      "time": "1/30/2026, 2:05:41 PM",
      "say": "You’ve already reviewed the implementation plan.\n\n\nPlease now implement the full logic for the stubbed normalize_fields(df, schema) function found in consolidate_cad.py.\n\n\nThe function should be moved to a new module:\nshared/processors/field_normalizer.py\n\n\nGoals:\n\t•\tExtract and adapt the Advanced Normalization Rules v3.2 from the legacy enhanced_esri_output_generator.py\n\t•\tSupport normalization for:\n\t•\tConcatenated values (e.g., \"DispersedComplete\" → \"Dispersed\")\n\t•\tPattern-based domain normalization (e.g., \"R\" → \"Radio\" in HowReported)\n\t•\tUSPS-compliant address formatting (FullAddress2)\n\t•\tField value defaults and coercions\n\t•\tIntegrate field mapping logic from transformation_spec.json and mapping_rules.md\n\n\nNotes:\n\t•\tNormalize all fields as string\n\t•\tAll logic should preserve original values in raw* columns where applicable\n\t•\tThe input schema contains canonical field names and accepted formats\n\n\nReturn the full Python module code for field_normalizer.py." },
    {
      "role": "Response",
      "time": "1/30/2026, 3:04:22 PM",
      "say": "Thought process: Architected comprehensive field normalizer module integrating legacy rules and validation schemas.\n\nThe user wants me to create a comprehensive field normalizer module based on the \"Advanced Normalization Rules v3.2\" mentioned in the project documentation. Let me review what I know about the requirements:\n\nFrom the documents:\n1. **Advanced Normalization Rules v3.2** includes:\n   - Concatenated value handling (\"DispersedComplete\" → \"Dispersed\")\n   - Pattern-based normalization (HowReported: \"R\" → \"Radio\", \"P\" → \"Phone\")\n   - Default assignment for 100% domain compliance\n\n2. **Domain validation from validation_rules.yaml**:\n   - HowReported valid values: 9-1-1, Phone, Walk-In, Self-Initiated, Radio, Online, Referral, Alarm, Other\n   - Normalize patterns: \"R\" → \"Radio\", \"P\" → \"Phone\", \"W\" → \"Walk-In\", \"S\" → \"Self-Initiated\", \"911\" → \"9-1-1\"\n   - Disposition: allow concatenated, split_pattern: \"(?=[A-Z][a-z])\"\n\n3. **Address standardization**:\n   - USPS pattern validation\n   - Use `usaddress` library (but that's more for address_standardizer.py - here I should do basic formatting)\n\n4. **Field mappings from transformation_spec.json**:\n   - 8-stage ETL pipeline\n   - mapping_rules.md has 7 mapping types\n\n5. **Key fields to normalize**:\n   - ReportNumberNew (case number - preserve as text)\n   - HowReported (domain normalization)\n   - Disposition (concatenated values)\n   - FullAddress2 (address formatting)\n   - PDZone\n   - TimeOfCall, DateOfCall (datetime fields)\n   - Incident (call type)\n\nLet me create a comprehensive field_normalizer.py module that:\n\n\n- Implements a FieldNormalizer class with configurable rules loaded from config/validation_rules.yaml\n- Supports transformation specs from schemas\n- Handles concatenated values, pattern-based mappings, and domain validation\n- Stores original values in _raw_* columns for audit trails\n- Includes specific normalizers for different field types (domain, datetime, address, text)\n- Provides comprehensive logging and error handling\n- Offers convenience functions for common normalization tasks\n\n\n# // 2026-01-30-11-15-00 EST\n# // cad_rms_data_quality/shared/processors/field_normalizer.py\n# // Author: R. A. Carucci\n# // Purpose: Advanced field normalization (v3.2) for CAD/RMS data with pattern-based\n# //          domain mapping, concatenated value handling, and USPS address formatting\n\n\"\"\"\nField Normalizer Module - Advanced Normalization Rules v3.2\n\nProvides comprehensive field normalization for CAD/RMS exports including:\n- Concatenated value splitting (\"DispersedComplete\" → \"Dispersed\")\n- Pattern-based domain normalization (\"R\" → \"Radio\")\n- USPS-compliant address formatting\n- Column name standardization\n- Default value assignment for domain compliance\n- Original value preservation in _raw_* columns\n\nUsage:\n    from shared.processors.field_normalizer import FieldNormalizer, normalize_fields\n    \n    normalizer = FieldNormalizer()\n    df_normalized = normalizer.normalize(df)\n    \n    # Or use convenience function\n    df_normalized = normalize_fields(df, schema)\n\"\"\"\n\nimport logging\nimport re\nfrom dataclasses import dataclass, field\nfrom enum import Enum\nfrom pathlib import Path\nfrom typing import Any, Callable, Dict, List, Optional, Set, Tuple, Union\n\nimport pandas as pd\n\nlogger = logging.getLogger(__name__)\n\n\n# =============================================================================\n# Enums and Data Classes\n# =============================================================================\n\nclass NormalizationType(Enum):\n    \"\"\"Types of normalization operations.\"\"\"\n    PATTERN_REPLACE = \"pattern_replace\"      # Regex pattern replacement\n    DOMAIN_MAP = \"domain_map\"                # Map values to canonical domain\n    CONCATENATED_SPLIT = \"concat_split\"      # Split concatenated values\n    CASE_TRANSFORM = \"case_transform\"        # Upper/lower/title case\n    STRIP_WHITESPACE = \"strip\"               # Trim whitespace\n    DEFAULT_VALUE = \"default\"                # Assign default if null/invalid\n    ADDRESS_FORMAT = \"address\"               # USPS address formatting\n    DATETIME_FORMAT = \"datetime\"             # Datetime standardization\n    NUMERIC_COERCE = \"numeric\"               # Coerce to numeric type\n    PRESERVE_TEXT = \"preserve_text\"          # Ensure string type (e.g., case numbers)\n\n\n@dataclass\nclass NormalizationRule:\n    \"\"\"\n    Defines a single normalization rule for a field.\n    \n    Attributes:\n        field: Target field name (or pattern for wildcard matching)\n        rule_type: Type of normalization to apply\n        params: Parameters specific to the rule type\n        priority: Execution order (lower = earlier, default 50)\n        preserve_original: Whether to keep original value in _raw_* column\n        description: Human-readable description\n    \"\"\"\n    field: str\n    rule_type: NormalizationType\n    params: Dict[str, Any] = field(default_factory=dict)\n    priority: int = 50\n    preserve_original: bool = False\n    description: str = \"\"\n    \n    def __post_init__(self):\n        if isinstance(self.rule_type, str):\n            self.rule_type = NormalizationType(self.rule_type)\n\n\n@dataclass\nclass NormalizationResult:\n    \"\"\"Results from a normalization operation.\"\"\"\n    field: str\n    records_processed: int\n    records_modified: int\n    null_before: int\n    null_after: int\n    unique_before: int\n    unique_after: int\n    errors: List[str] = field(default_factory=list)\n    sample_changes: List[Tuple[Any, Any]] = field(default_factory=list)\n\n\n# =============================================================================\n# Default Normalization Rules - Advanced Rules v3.2\n# =============================================================================\n\n# Pattern-based domain mappings for HowReported field\nHOW_REPORTED_MAPPINGS: Dict[str, str] = {\n    # Abbreviations\n    \"R\": \"Radio\",\n    \"P\": \"Phone\",\n    \"W\": \"Walk-In\",\n    \"S\": \"Self-Initiated\",\n    \"O\": \"Online\",\n    \"A\": \"Alarm\",\n    \"T\": \"Transfer\",\n    # Common variations\n    \"911\": \"9-1-1\",\n    \"9-1-1\": \"9-1-1\",\n    \"PHONE\": \"Phone\",\n    \"RADIO\": \"Radio\",\n    \"WALK-IN\": \"Walk-In\",\n    \"WALKIN\": \"Walk-In\",\n    \"WALK IN\": \"Walk-In\",\n    \"SELF-INITIATED\": \"Self-Initiated\",\n    \"SELF INITIATED\": \"Self-Initiated\",\n    \"SELFINIT\": \"Self-Initiated\",\n    \"ONLINE\": \"Online\",\n    \"WEB\": \"Online\",\n    \"ALARM\": \"Alarm\",\n    \"REFERRAL\": \"Referral\",\n    \"REF\": \"Referral\",\n    \"OTHER\": \"Other\",\n    \"OTH\": \"Other\",\n    \"TRANSFER\": \"Transfer\",\n    \"XFER\": \"Transfer\",\n}\n\n# Valid domain values for HowReported\nHOW_REPORTED_VALID: Set[str] = {\n    \"9-1-1\", \"Phone\", \"Walk-In\", \"Self-Initiated\", \n    \"Radio\", \"Online\", \"Referral\", \"Alarm\", \"Other\", \"Transfer\"\n}\n\n# Concatenated value patterns (CamelCase splits)\nCONCATENATED_PATTERNS: Dict[str, Dict[str, str]] = {\n    \"Disposition\": {\n        \"DispersedComplete\": \"Dispersed\",\n        \"ArrestedComplete\": \"Arrested\",\n        \"ClearedComplete\": \"Cleared\",\n        \"UnfoundedComplete\": \"Unfounded\",\n        \"ReportComplete\": \"Report\",\n        \"GoneOnArrival\": \"GOA\",\n        \"GOAComplete\": \"GOA\",\n        \"NoActionRequired\": \"No Action Required\",\n        \"ReferredOther\": \"Referred\",\n        \"WarningIssued\": \"Warning\",\n        \"CitationIssued\": \"Citation\",\n        \"SummonIssued\": \"Summons\",\n        \"SummonsIssued\": \"Summons\",\n        \"TransportedHospital\": \"Transported\",\n        \"TransportedJail\": \"Transported\",\n        \"CancelledDuplicate\": \"Cancelled\",\n        \"CancelledError\": \"Cancelled\",\n    }\n}\n\n# USPS address abbreviation mappings\nUSPS_STREET_SUFFIXES: Dict[str, str] = {\n    \"STREET\": \"ST\",\n    \"AVENUE\": \"AVE\",\n    \"BOULEVARD\": \"BLVD\",\n    \"DRIVE\": \"DR\",\n    \"LANE\": \"LN\",\n    \"ROAD\": \"RD\",\n    \"COURT\": \"CT\",\n    \"CIRCLE\": \"CIR\",\n    \"PLACE\": \"PL\",\n    \"TERRACE\": \"TER\",\n    \"HIGHWAY\": \"HWY\",\n    \"PARKWAY\": \"PKWY\",\n    \"EXPRESSWAY\": \"EXPY\",\n    \"TURNPIKE\": \"TPKE\",\n    \"SQUARE\": \"SQ\",\n    \"TRAIL\": \"TRL\",\n    \"WAY\": \"WAY\",\n    \"ALLEY\": \"ALY\",\n}\n\nUSPS_DIRECTIONALS: Dict[str, str] = {\n    \"NORTH\": \"N\",\n    \"SOUTH\": \"S\",\n    \"EAST\": \"E\",\n    \"WEST\": \"W\",\n    \"NORTHEAST\": \"NE\",\n    \"NORTHWEST\": \"NW\",\n    \"SOUTHEAST\": \"SE\",\n    \"SOUTHWEST\": \"SW\",\n}\n\n# Column name standardization mappings\nCOLUMN_NAME_MAPPINGS: Dict[str, str] = {\n    # Common variations → canonical names\n    \"report_number\": \"ReportNumberNew\",\n    \"reportnumber\": \"ReportNumberNew\",\n    \"report_num\": \"ReportNumberNew\",\n    \"case_number\": \"CaseNumber\",\n    \"casenumber\": \"CaseNumber\",\n    \"case_num\": \"CaseNumber\",\n    \"incident_type\": \"Incident\",\n    \"incidenttype\": \"Incident\",\n    \"call_type\": \"Incident\",\n    \"calltype\": \"Incident\",\n    \"time_of_call\": \"TimeOfCall\",\n    \"timeofcall\": \"TimeOfCall\",\n    \"call_time\": \"TimeOfCall\",\n    \"date_of_call\": \"DateOfCall\",\n    \"dateofcall\": \"DateOfCall\",\n    \"call_date\": \"DateOfCall\",\n    \"full_address\": \"FullAddress2\",\n    \"fulladdress\": \"FullAddress2\",\n    \"address\": \"FullAddress2\",\n    \"location\": \"FullAddress2\",\n    \"pd_zone\": \"PDZone\",\n    \"pdzone\": \"PDZone\",\n    \"zone\": \"PDZone\",\n    \"beat\": \"PDZone\",\n    \"how_reported\": \"HowReported\",\n    \"howreported\": \"HowReported\",\n    \"source\": \"HowReported\",\n    \"disposition\": \"Disposition\",\n    \"disp\": \"Disposition\",\n    \"status\": \"Disposition\",\n    \"officer\": \"Officer\",\n    \"responding_officer\": \"Officer\",\n    \"primary_officer\": \"Officer\",\n}\n\n# Fields that must be preserved as text (not converted to numeric)\nTEXT_PRESERVE_FIELDS: Set[str] = {\n    \"ReportNumberNew\",\n    \"CaseNumber\",\n    \"PDZone\",\n    \"FullAddress2\",\n    \"Incident\",\n    \"Officer\",\n    \"BadgeNumber\",\n}\n\n\n# =============================================================================\n# Normalizer Functions\n# =============================================================================\n\ndef normalize_column_names(df: pd.DataFrame) -> Tuple[pd.DataFrame, Dict[str, str]]:\n    \"\"\"\n    Standardize column names to canonical format.\n    \n    Args:\n        df: Input DataFrame\n        \n    Returns:\n        Tuple of (normalized DataFrame, mapping of old → new names)\n    \"\"\"\n    rename_map = {}\n    \n    for col in df.columns:\n        # Clean the column name\n        clean = col.strip()\n        \n        # Check for direct mapping (case-insensitive)\n        lower = clean.lower().replace(\" \", \"_\").replace(\"-\", \"_\")\n        if lower in COLUMN_NAME_MAPPINGS:\n            rename_map[col] = COLUMN_NAME_MAPPINGS[lower]\n        elif clean != col:\n            # At minimum, strip whitespace\n            rename_map[col] = clean\n    \n    if rename_map:\n        df = df.rename(columns=rename_map)\n        logger.debug(f\"Renamed {len(rename_map)} columns: {rename_map}\")\n    \n    return df, rename_map\n\n\ndef normalize_how_reported(series: pd.Series) -> pd.Series:\n    \"\"\"\n    Normalize HowReported field using pattern-based domain mapping.\n    \n    Args:\n        series: Pandas Series containing HowReported values\n        \n    Returns:\n        Normalized Series with canonical domain values\n    \"\"\"\n    def _normalize_value(val: Any) -> Optional[str]:\n        if pd.isna(val) or val is None:\n            return None\n        \n        # Convert to string and clean\n        s = str(val).strip().upper()\n        \n        if not s:\n            return None\n        \n        # Check direct mapping\n        if s in HOW_REPORTED_MAPPINGS:\n            return HOW_REPORTED_MAPPINGS[s]\n        \n        # Check if already valid (case-insensitive)\n        for valid in HOW_REPORTED_VALID:\n            if s == valid.upper():\n                return valid\n        \n        # Partial matching for common patterns\n        if \"911\" in s or \"9-1-1\" in s:\n            return \"9-1-1\"\n        if \"PHONE\" in s or \"CALL\" in s:\n            return \"Phone\"\n        if \"WALK\" in s:\n            return \"Walk-In\"\n        if \"SELF\" in s or \"OFFICER\" in s:\n            return \"Self-Initiated\"\n        if \"RADIO\" in s:\n            return \"Radio\"\n        if \"ALARM\" in s:\n            return \"Alarm\"\n        if \"WEB\" in s or \"ONLINE\" in s or \"INTERNET\" in s:\n            return \"Online\"\n        if \"REFER\" in s:\n            return \"Referral\"\n        \n        # Default to \"Other\" for unrecognized values\n        logger.debug(f\"HowReported value '{val}' mapped to 'Other'\")\n        return \"Other\"\n    \n    return series.apply(_normalize_value)\n\n\ndef normalize_disposition(series: pd.Series) -> pd.Series:\n    \"\"\"\n    Normalize Disposition field, handling concatenated CamelCase values.\n    \n    Args:\n        series: Pandas Series containing Disposition values\n        \n    Returns:\n        Normalized Series with split/mapped values\n    \"\"\"\n    concat_map = CONCATENATED_PATTERNS.get(\"Disposition\", {})\n    \n    def _normalize_value(val: Any) -> Optional[str]:\n        if pd.isna(val) or val is None:\n            return None\n        \n        s = str(val).strip()\n        if not s:\n            return None\n        \n        # Check direct mapping for known concatenated values\n        if s in concat_map:\n            return concat_map[s]\n        \n        # Try case-insensitive match\n        s_upper = s.upper()\n        for key, mapped in concat_map.items():\n            if key.upper() == s_upper:\n                return mapped\n        \n        # Split CamelCase if it looks concatenated (no spaces, mixed case)\n        if \" \" not in s and any(c.isupper() for c in s[1:]):\n            # Split on capital letters\n            parts = re.findall(r'[A-Z][a-z]*', s)\n            if len(parts) > 1:\n                # Take first meaningful part (often the main disposition)\n                first_part = parts[0]\n                # Check if first part is in our mapping values\n                for mapped in concat_map.values():\n                    if first_part.lower() == mapped.lower():\n                        return mapped\n                return first_part\n        \n        # Return as-is with title case\n        return s.title() if s.islower() or s.isupper() else s\n    \n    return series.apply(_normalize_value)\n\n\ndef normalize_address_usps(series: pd.Series) -> pd.Series:\n    \"\"\"\n    Apply USPS-compliant address formatting.\n    \n    Args:\n        series: Pandas Series containing address values\n        \n    Returns:\n        Normalized Series with USPS standard formatting\n    \"\"\"\n    def _normalize_address(val: Any) -> Optional[str]:\n        if pd.isna(val) or val is None:\n            return None\n        \n        s = str(val).strip()\n        if not s:\n            return None\n        \n        # Convert to uppercase for processing\n        addr = s.upper()\n        \n        # Replace street suffixes with abbreviations\n        for full, abbrev in USPS_STREET_SUFFIXES.items():\n            # Match whole word only\n            pattern = rf'\\b{full}\\b\\.? '\n            addr = re.sub(pattern, abbrev, addr, flags=re.IGNORECASE)\n        \n        # Replace directionals with abbreviations\n        for full, abbrev in USPS_DIRECTIONALS.items():\n            pattern = rf'\\b{full}\\b\\.? '\n            addr = re.sub(pattern, abbrev, addr, flags=re.IGNORECASE)\n        \n        # Remove extra whitespace\n        addr = re.sub(r'\\s+', ' ', addr).strip()\n        \n        # Remove trailing periods\n        addr = addr.rstrip('. ')\n        \n        # Standardize apartment/unit notation\n        addr = re.sub(r'\\bAPT\\. ?\\s*#?\\s*', 'APT ', addr)\n        addr = re.sub(r'\\bUNIT\\. ?\\s*#?\\s*', 'UNIT ', addr)\n        addr = re.sub(r'\\bSTE\\. ?\\s*#?\\s*', 'STE ', addr)\n        addr = re.sub(r'\\bSUITE\\. ?\\s*#?\\s*', 'STE ', addr)\n        addr = re.sub(r'\\bFLOOR\\. ?\\s*#?\\s*', 'FL ', addr)\n        addr = re.sub(r'\\bFL\\. ?\\s*#?\\s*', 'FL ', addr)\n        \n        # Remove duplicate spaces again\n        addr = re.sub(r'\\s+', ' ', addr).strip()\n        \n        return addr\n    \n    return series.apply(_normalize_address)\n\n\ndef normalize_case_number(series: pd.Series) -> pd.Series:\n    \"\"\"\n    Normalize case/report numbers preserving as text.\n    \n    Handles formats like: 25-000001, 25-000001A, 2025-000001\n    \n    Args:\n        series: Pandas Series containing case numbers\n        \n    Returns:\n        Normalized Series with standardized case number format\n    \"\"\"\n    def _normalize_case(val: Any) -> Optional[str]:\n        if pd.isna(val) or val is None:\n            return None\n        \n        s = str(val).strip()\n        if not s:\n            return None\n        \n        # Remove any leading/trailing quotes or whitespace\n        s = s.strip(\"'\\\"` \")\n        \n        # Already in correct format (YY-NNNNNN or YY-NNNNNNA)\n        if re.match(r'^\\d{2}-\\d{6}[A-Z]?$', s):\n            return s.upper()\n        \n        # Handle 4-digit year format (YYYY-NNNNNN)\n        match = re.match(r'^(20)?