Deduplicate and export with metadata\n\nUsage:\n    python -m consolidation.scripts.consolidate_cad\n    python -m consolidation.scripts.consolidate_cad --dry-run\n    python -m consolidation.scripts.consolidate_cad --quality-threshold 90\n\"\"\"\n\nimport argparse\nimport hashlib\nimport json\nimport logging\nimport sys\nfrom datetime import datetime\nfrom pathlib import Path\nfrom typing import Any, Dict, List, Optional, Tuple\n\nimport pandas as pd\n\n# Add project root to path for imports\nPROJECT_ROOT = Path(__file__).resolve().parent.parent.parent\nsys.path.insert(0, str(PROJECT_ROOT))\n\nfrom shared.utils.schema_loader import ConfigLoader, load_config, load_schema\nfrom shared.processors.field_normalizer import normalize_fields, FieldNormalizer\nfrom shared.validators.validation_engine import ValidationEngine, validate_dataframe\nfrom shared.validators.quality_scorer import QualityScorer, QualityTier\n\n# Configure logging\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s - %(levelname)s - %(name)s - %(message)s\",\n    handlers=[\n        logging.StreamHandler(sys.stdout),\n    ]\n)\nlogger = logging.getLogger(__name__)\n\n\n# =============================================================================\n# Constants\n# =============================================================================\n\nDEFAULT_QUALITY_THRESHOLD = 95.0  # For consolidated historical data\nCHUNK_SIZE = 50000\nTEXT_PRESERVE_FIELDS = {\n    \"ReportNumberNew\", \"CaseNumber\", \"PDZone\", \"FullAddress2\",\n    \"Incident\", \"Officer\", \"BadgeNumber\", \"Location\",\n}\n\n\n# =============================================================================\n# Pipeline Functions\n# =============================================================================\n\ndef compute_file_hash(filepath: Path) -> str:\n    \"\"\"Compute MD5 hash of file for audit trail.\"\"\"\n    hash_md5 = hashlib.md5()\n    with open(filepath, \"rb\") as f:\n        for chunk in iter(lambda: f.read(8192), b\"\"):\n            hash_md5.update(chunk)\n    return hash_md5.hexdigest()\n\n\ndef load_source_files(\n    sources_config: Dict[str, Any],\n    dry_run: bool = False,\n) -> Tuple[pd.DataFrame, Dict[str, Any]]:\n    \"\"\"\n    Load all source CAD files from configuration.\n    \n    Args:\n        sources_config: Configuration dict with source file paths\n        dry_run: If True, only load first 1000 rows per file\n        \n    Returns:\n        Tuple of (combined DataFrame, load metrics dict)\n    \"\"\"\n    source_files = sources_config.get(\"source_files\", [])\n    \n    if not source_files:\n        raise ValueError(\"No source files configured\")\n    \n    logger.info(f\"Loading {len(source_files)} source files...\")\n    \n    dfs = []\n    load_metrics = {\n        \"files_loaded\": 0,\n        \"files_failed\": [],\n        \"file_details\": {},\n        \"total_records_loaded\": 0,\n    }\n    \n    for source in source_files:\n        filepath = Path(source[\"path\"])\n        year = source.get(\"year\", \"unknown\")\n        expected_count = source.get(\"expected_records\", 0)\n        \n        if not filepath.exists():\n            logger.warning(f\"Source file not found: {filepath}\")\n            load_metrics[\"files_failed\"].append({\n                \"path\": str(filepath),\n                \"year\": year,\n                \"error\": \"File not found\",\n            })\n            continue\n        \n        try:\n            # Determine read parameters\n            nrows = 1000 if dry_run else None\n            \n            if filepath.suffix in ['.xlsx', '.xls']:\n                df = pd.read_excel(filepath, dtype=str, nrows=nrows)\n            else:\n                df = pd.read_csv(filepath, dtype=str, nrows=nrows, low_memory=False)\n            \n            # Add source metadata columns\n            df[\"_source_file\"] = filepath.name\n            df[\"_source_year\"] = str(year)\n            \n            # Compute file hash\n            file_hash = compute_file_hash(filepath)\n            \n            record_count = len(df)\n            dfs.append(df)\n            \n            load_metrics[\"files_loaded\"] += 1\n            load_metrics[\"total_records_loaded\"] += record_count\n            load_metrics[\"file_details\"][filepath.name] = {\n                \"year\": year,\n                \"records\": record_count,\n                \"expected\": expected_count,\n                \"variance\": record_count - expected_count if expected_count else None,\n                \"hash\": file_hash,\n                \"columns\": len(df.columns),\n            }\n            \n            logger.info(f\"  ✓ {filepath.name}: {record_count:,} records (year {year})\")\n            \n        except Exception as e:\n            logger.error(f\"  ✗ Failed to load {filepath}: {e}\")\n            load_metrics[\"files_failed\"].append({\n                \"path\": str(filepath),\n                \"year\": year,\n                \"error\": str(e),\n            })\n    \n    if not dfs:\n        raise ValueError(\"No source files could be loaded\")\n    \n    # Concatenate all DataFrames\n    logger.info(\"Concatenating source files...\")\n    df_combined = pd.concat(dfs, ignore_index=True, sort=False)\n    \n    logger.info(f\"Combined dataset: {len(df_combined):,} records, {len(df_combined.columns)} columns\")\n    \n    return df_combined, load_metrics\n\n\ndef detect_gaps(\n    df: pd.DataFrame,\n    date_field: str = \"DateOfCall\",\n    min_daily_records: int = 50,\n) -> List[Dict[str, Any]]:\n    \"\"\"\n    Detect gaps in date coverage.\n    \n    Args:\n        df: DataFrame to analyze\n        date_field: Name of date field\n        min_daily_records: Minimum expected records per day\n        \n    Returns:\n        List of detected gaps\n    \"\"\"\n    gaps = []\n    \n    if date_field not in df.columns:\n        logger.warning(f\"Date field '{date_field}' not found for gap detection\")\n        return gaps\n    \n    try:\n        # Parse dates\n        dates = pd.to_datetime(df[date_field], errors='coerce')\n        valid_dates = dates.dropna()\n        \n        if valid_dates.empty:\n            return gaps\n        \n        # Daily record counts\n        daily_counts = valid_dates.dt.date.value_counts().sort_index()\n        \n        # Check for missing dates\n        date_range = pd.date_range(\n            start=daily_counts.index.min(),\n            end=daily_counts.index.max(),\n            freq='D'\n        )\n        \n        for date in date_range:\n            date_key = date.date()\n            count = daily_counts.get(date_key, 0)\n            \n            if count == 0:\n                gaps.append({\n                    \"date\": str(date_key),\n                    \"type\": \"missing\",\n                    \"records\": 0,\n                })\n            elif count < min_daily_records:\n                gaps.append({\n                    \"date\": str(date_key),\n                    \"type\": \"low_count\",\n                    \"records\": int(count),\n                    \"expected_min\": min_daily_records,\n                })\n        \n        logger.info(f\"Gap detection: {len(gaps)} gaps identified\")\n        \n    except Exception as e:\n        logger.warning(f\"Gap detection failed: {e}\")\n    \n    return gaps\n\n\ndef deduplicate_records(\n    df: pd.DataFrame,\n    key_field: str = \"ReportNumberNew\",\n    sort_by: str = \"_source_year\",\n) -> Tuple[pd.DataFrame, Dict[str, Any]]:\n    \"\"\"\n    Deduplicate records by key field, keeping earliest occurrence.\n    \n    Args:\n        df: DataFrame to deduplicate\n        key_field: Field to use as unique key\n        sort_by: Field to sort by before dedup (keep first)\n        \n    Returns:\n        Tuple of (deduplicated DataFrame, dedup metrics)\n    \"\"\"\n    initial_count = len(df)\n    \n    if key_field not in df.columns:\n        logger.warning(f\"Key field '{key_field}' not found, skipping deduplication\")\n        return df, {\"skipped\": True, \"reason\": \"Key field not found\"}\n    \n    # Sort by source year to keep earliest\n    if sort_by in df.columns:\n        df = df.sort_values(sort_by, ascending=True)\n    \n    # Drop duplicates\n    df_deduped = df.drop_duplicates(subset=[key_field], keep='first')\n    \n    final_count = len(df_deduped)\n    duplicates_removed = initial_count - final_count\n    \n    metrics = {\n        \"initial_records\": initial_count,\n        \"final_records\": final_count,\n        \"duplicates_removed\": duplicates_removed,\n        \"duplicate_rate\": round(duplicates_removed / initial_count * 100, 2) if initial_count > 0 else 0,\n        \"key_field\": key_field,\n    }\n    \n    logger.info(f\"Deduplication: {duplicates_removed:,} duplicates removed ({metrics['duplicate_rate']}%)\")\n    \n    return df_deduped, metrics\n\n\ndef export_results(\n    df: pd.DataFrame,\n    output_dir: Path,\n    quality_score: float,\n    metadata: Dict[str, Any],\n    dry_run: bool = False,\n) -> Dict[str, Path]:\n    \"\"\"\n    Export consolidated data and metadata.\n    \n    Args:\n        df: Final DataFrame to export\n        output_dir: Output directory path\n        quality_score: Quality score for filename\n        metadata: Pipeline metadata dict\n        dry_run: If True, skip actual file writing\n        \n    Returns:\n        Dict of output file paths\n    \"\"\"\n    output_dir = Path(output_dir)\n    output_dir.mkdir(parents=True, exist_ok=True)\n    \n    # Generate quality tier for filename\n    tier = QualityTier.from_score(quality_score).value.upper()\n    \n    # Output filenames\n    timestamp = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n    csv_filename = f\"2019_2026_CAD_Consolidated_QUALITY_{tier}_{int(quality_score)}.csv\"\n    metadata_filename = \"consolidation_metadata.json\"\n    gap_filename = \"gap_analysis.json\"\n    \n    output_files = {\n        \"consolidated_csv\": output_dir / csv_filename,\n        \"metadata_json\": output_dir / metadata_filename,\n        \"gap_analysis\": output_dir / gap_filename,\n    }\n    \n    if dry_run:\n        logger.info(f\"DRY RUN - Would export to:\")\n        for name, path in output_files.items():\n            logger.info(f\"  {name}: {path}\")\n        return output_files\n    \n    # Export CSV\n    logger.info(f\"Exporting consolidated CSV: {csv_filename}\")\n    \n    # Remove internal columns before export\n    export_df = df.drop(columns=[c for c in df.columns if c.startswith('_')], errors='ignore')\n    export_df.to_csv(output_files[\"consolidated_csv\"], index=False)\n    \n    # Export metadata\n    logger.info(f\"Exporting metadata: {metadata_filename}\")\n    metadata[\"output_files\"] = {k: str(v) for k, v in output_files.items()}\n    metadata[\"export_timestamp\"] = datetime.now().isoformat()\n    \n    with open(output_files[\"metadata_json\"], 'w', encoding='utf-8') as f:\n        json.dump(metadata, f, indent=2, default=str)\n    \n    # Export gap analysis\n    if \"gaps\" in metadata:\n        with open(output_files[\"gap_analysis\"], 'w', encoding='utf-8') as f:\n            json.dump({\"gaps\": metadata[\"gaps\"]}, f, indent=2)\n    \n    logger.info(f\"Export complete: {len(export_df):,} records\")\n    \n    return output_files\n\n\n# =============================================================================\n# Main Pipeline\n# =============================================================================\n\ndef run_consolidation_pipeline(\n    config_dir: Optional[Path] = None,\n    output_dir: Optional[Path] = None,\n    quality_threshold: float = DEFAULT_QUALITY_THRESHOLD,\n    dry_run: bool = False,\n    validate_only: bool = False,\n    verbose: bool = False,\n) -> Tuple[bool, Dict[str, Any]]:\n    \"\"\"\n    Execute the full consolidation pipeline.\n    \n    Args:\n        config_dir: Path to config directory\n        output_dir: Path to output directory\n        quality_threshold: Minimum quality score required\n        dry_run: If True, run without writing outputs\n        validate_only: If True, only run validation\n        verbose: Enable verbose logging\n        \n    Returns:\n        Tuple of (success bool, results dict)\n    \"\"\"\n    start_time = datetime.now()\n    \n    if verbose:\n        logging.getLogger().setLevel(logging.DEBUG)\n    \n    logger.info(\"=\" * 60)\n    logger.info(\"CAD DATA CONSOLIDATION PIPELINE\")\n    logger.info(\"=\" * 60)\n    logger.info(f\"Start time: {start_time.isoformat()}\")\n    logger.info(f\"Quality threshold: {quality_threshold}\")\n    logger.info(f\"Dry run: {dry_run}\")\n    \n    # Initialize results\n    results = {\n        \"success\": False,\n        \"start_time\": start_time.isoformat(),\n        \"quality_threshold\": quality_threshold,\n        \"dry_run\": dry_run,\n    }\n    \n    try:\n        # =====================================================================\n        # STEP 1: Load Configuration\n        # =====================================================================\n        logger.info(\"\\n[STEP 1/6] Loading configuration...\")\n        \n        if config_dir is None:\n            config_dir = PROJECT_ROOT / \"config\"\n        \n        config_loader = ConfigLoader(config_dir)\n        \n        # Load configs\n        sources_config = config_loader.load_yaml(\"consolidation_sources.yaml\")\n        validation_config = config_loader.load_yaml(\"validation_rules.yaml\")\n        schemas_config = config_loader.load_yaml(\"schemas.yaml\")\n        \n        # Load CAD schema\n        cad_schema_path = schemas_config.get(\"field_schemas\", {}).get(\"cad\")\n        if cad_schema_path:\n            cad_schema = config_loader.load_schema(cad_schema_path)\n        else:\n            cad_schema = {}\n            logger.warning(\"CAD schema not found, using defaults\")\n        \n        results[\"config_loaded\"] = True\n        logger.info(\"  ✓ Configuration loaded\")\n        \n        # =====================================================================\n        # STEP 2: Load Source Files\n        # =====================================================================\n        logger.info(\"\\n[STEP 2/6] Loading source files...\")\n        \n        df_combined, load_metrics = load_source_files(sources_config, dry_run=dry_run)\n        \n        results[\"load_metrics\"] = load_metrics\n        results[\"records_loaded\"] = len(df_combined)\n        \n        logger.info(f\"  ✓ Loaded {len(df_combined):,} records from {load_metrics['files_loaded']} files\")\n        \n        if load_metrics[\"files_failed\"]:\n            logger.warning(f\"  ⚠ {len(load_metrics['files_failed'])} files failed to load\")\n        \n        # =====================================================================\n        # STEP 3: Normalize Fields\n        # =====================================================================\n        logger.info(\"\\n[STEP 3/6] Normalizing fields...\")\n        \n        normalizer = FieldNormalizer(config_path=config_dir / \"validation_rules.yaml\")\n        df_normalized = normalizer.normalize(df_combined, preserve_originals=True)\n        \n        normalization_results = normalizer.get_results()\n        normalization_summary = {\n            field: {\n                \"records_modified\": r.records_modified,\n                \"null_before\": r.null_before,\n                \"null_after\": r.null_after,\n            }\n            for field, r in normalization_results.items()\n        }\n        \n        results[\"normalization\"] = {\n            \"fields_normalized\": len(normalization_results),\n            \"summary\": normalization_summary,\n        }\n        \n        logger.info(f\"  ✓ Normalized {len(normalization_results)} fields\")\n        logger.info(normalizer.get_summary())\n        \n        # =====================================================================\n        # STEP 4: Validate Data\n        # =====================================================================\n        logger.info(\"\\n[STEP 4/6] Validating data...\")\n        \n        validator = ValidationEngine(\n            schema=cad_schema,\n            config_path=config_dir / \"validation_rules.yaml\",\n        )\n        validation_result = validator.validate(df_normalized)\n        \n        results[\"validation\"] = validation_result.to_dict()\n        \n        logger.info(validator.get_summary())\n        \n        # Check validation errors\n        if validation_result.error_count > 0:\n            logger.error(f\"  ✗ Validation failed with {validation_result.error_count} errors\")\n            \n            if not dry_run and not validate_only:\n                results[\"failure_reason\"] = \"Validation errors detected\"\n                results[\"success\"] = False\n                \n                # Still calculate quality for reporting\n                logger.info(\"\\n[STEP 5/6] Calculating quality score (for reporting)...\")\n                scorer = QualityScorer(threshold=quality_threshold)\n                quality_result = scorer.score(df_normalized, validation_result.to_dict())\n                results[\"quality\"] = quality_result.to_dict()\n                logger.info(scorer.get_summary())\n                \n                return False, results\n        \n        logger.info(f\"  ✓ Validation complete: {validation_result.valid_records:,} valid records\")\n        \n        if validate_only:\n            results[\"success\"] = True\n            results[\"validate_only\"] = True\n            return True, results\n        \n        # =====================================================================\n        # STEP 5: Calculate Quality Score\n        # =====================================================================\n        logger.info(\"\\n[STEP 5/6] Calculating quality score...\")\n        \n        scorer = QualityScorer(\n            threshold=quality_threshold,\n            config_path=config_dir / \"validation_rules.yaml\",\n        )\n        quality_result = scorer.score(df_normalized, validation_result.to_dict())\n        \n        results[\"quality\"] = quality_result.to_dict()\n        \n        logger.info(scorer.get_summary())\n        \n        # Check quality threshold\n        mean_score = quality_result.dataset_score.mean_score\n        \n        if mean_score < quality_threshold:\n            logger.error(\n                f\"  ✗ Quality score {mean_score} below threshold {quality_threshold}\"\n            )\n            results[\"failure_reason\"] = f\"Quality score {mean_score} below threshold {quality_threshold}\"\n            results[\"success\"] = False\n            return False, results\n        \n        logger.info(f\"  ✓ Quality score: {mean_score} (threshold: {quality_threshold})\")\n        \n        # =====================================================================\n        # STEP 6: Deduplicate and Export\n        # =====================================================================\n        logger.info(\"\\n[STEP 6/6] Deduplicating and exporting...\")\n        \n        # Detect gaps before dedup\n        gaps = detect_gaps(df_normalized)\n        results[\"gaps\"] = gaps\n        \n        # Deduplicate\n        df_final, dedup_metrics = deduplicate_records(df_normalized)\n        results[\"deduplication\"] = dedup_metrics\n        \n        # Determine output directory\n        if output_dir is None:\n            output_config = sources_config.get(\"output\", {})\n            output_dir = Path(output_config.get(\"directory\", PROJECT_ROOT / \"outputs\" / \"consolidation\"))\n        \n        # Export results\n        output_files = export_results(\n            df=df_final,\n            output_dir=output_dir,\n            quality_score=mean_score,\n            metadata=results,\n            dry_run=dry_run,\n        )\n        \n        results[\"output_files\"] = {k: str(v) for k, v in output_files.items()}\n        results[\"records_output\"] = len(df_final)\n        \n        # =====================================================================\n        # Complete\n        # =====================================================================\n        end_time = datetime.now()\n        duration = (end_time - start_time).total_seconds()\n        \n        results[\"end_time\"] = end_time.isoformat()\n        results[\"duration_seconds\"] = duration\n        results[\"success\"] = True\n        \n        logger.info(\"\\n\" + \"=\" * 60)\n        logger.info(\"PIPELINE COMPLETE\")\n        logger.info(\"=\" * 60)\n        logger.info(f\"Duration:        {duration:.2f} seconds\")\n        logger.info(f\"Records loaded:  {results['records_loaded']:,}\")\n        logger.info(f\"Records output:  {results['records_output']:,}\")\n        logger.info(f\"Quality score:   {mean_score}\")\n        logger.info(f\"Quality tier:    {quality_result.dataset_score.tier.value.upper()}\")\n        logger.info(f\"Success:         ✓\")\n        \n        if not dry_run:\n            logger.info(f\"\\nOutput files:\")\n            for name, path in output_files.items():\n                logger.info(f\"  {name}: {path}\")\n        \n        return True, results\n        \n    except Exception as e:\n        logger.exception(f\"Pipeline failed with error: {e}\")\n        results[\"error\"] = str(e)\n        results[\"success\"] = False\n        \n        end_time = datetime.now()\n        results[\"end_time\"] = end_time.isoformat()\n        results[\"duration_seconds\"] = (end_time - start_time).total_seconds()\n        \n        return False, results\n\n\n# =============================================================================\n# CLI Interface\n# =============================================================================\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    parser = argparse.ArgumentParser(\n        description=\"Consolidate historical CAD data (2019-2026)\",\n        formatter_class=argparse.RawDescriptionHelpFormatter,\n        epilog=\"\"\"\nExamples:\n    # Run full consolidation\n    python -m consolidation.scripts.consolidate_cad\n    \n    # Dry run (no file output)\n    python -m consolidation.scripts.consolidate_cad --dry-run\n    \n    # Custom output directory\n    python -m consolidation.scripts.consolidate_cad --output-dir ./output\n    \n    # Custom quality threshold\n    python -m consolidation.scripts.consolidate_cad --quality-threshold 90\n    \n    # Validation only\n    python -m consolidation.scripts.consolidate_cad --validate-only\n        \"\"\"\n    )\n    \n    parser.add_argument(\n        \"--config-dir\", \"-c\",\n        type=Path,\n        help=\"Path to config directory (default: project/config)\",\n    )\n    parser.add_argument(\n        \"--output-dir\", \"-o\",\n        type=Path,\n        help=\"Path to output directory\",\n    )\n    parser.add_argument(\n        \"--quality-threshold\", \"-q\",\n        type=float,\n        default=DEFAULT_QUALITY_THRESHOLD,\n        help=f\"Minimum quality score required (default: {DEFAULT_QUALITY_THRESHOLD})\",\n    )\n    parser.add_argument(\n        \"--dry-run\", \"-n\",\n        action=\"store_true\",\n        help=\"Run without writing output files\",\n    )\n    parser.add_argument(\n        \"--validate-only\",\n        action=\"store_true\",\n        help=\"Only run validation, skip export\",\n    )\n    parser.add_argument(\n        \"--verbose\", \"-v\",\n        action=\"store_true\",\n        help=\"Enable verbose logging\",\n    )\n    parser.add_argument(\n        \"--json-output\",\n        type=Path,\n        help=\"Write results to JSON file\",\n    )\n    \n    args = parser.parse_args()\n    \n    # Run pipeline\n    success, results = run_consolidation_pipeline(\n        config_dir=args.config_dir,\n        output_dir=args.output_dir,\n        quality_threshold=args.quality_threshold,\n        dry_run=args.dry_run,\n        validate_only=args.validate_only,\n        verbose=args.verbose,\n    )\n    \n    # Write JSON output if requested\n    if args.json_output:\n        with open(args.json_output, 'w', encoding='utf-8') as f:\n            json.dump(results, f, indent=2, default=str)\n        logger.info(f\"Results written to {args.json_output}\")\n    \n    # Exit with appropriate code\n    sys.exit(0 if success else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n\n2️⃣ PIPELINE METADATA (Already integrated above, but here's the structure)\n\n\nThe consolidation_metadata.json produced by the pipeline:\n\n\n{\n  \"success\": true,\n  \"start_time\": \"2026-01-30T12:30:00\",\n  \"quality_threshold\": 95.0,\n  \"dry_run\": false,\n  \"records_loaded\": 234521,\n  \"load_metrics\": {\n    \"files_loaded\": 8,\n    \"files_failed\": [],\n    \"total_records_loaded\": 234521,\n    \"file_details\": {\n      \"2019_CAD_Export.xlsx\": {\n        \"year\": 2019,\n        \"records\": 28456,\n        \"expected\": 28000,\n        \"variance\": 456,\n        \"hash\": \"abc123...\",\n        \"columns\": 42\n      }\n    }\n  },\n  \"normalization\": {\n    \"fields_normalized\": 7,\n    \"summary\": {}\n  },\n  \"validation\": {\n    \"summary\": {\n      \"total_records\": 234521,\n      \"valid_records\": 232100,\n      \"error_count\": 0\n    }\n  },\n  \"quality\": {\n    \"summary\": {\n      \"mean_score\": 96.3,\n      \"tier\": \"excellent\",\n      \"pass_rate\": 98.2\n    }\n  },\n  \"deduplication\": {\n    \"initial_records\": 234521,\n    \"final_records\": 231456,\n    \"duplicates_removed\": 3065\n  },\n  \"gaps\": [],\n  \"output_files\": {\n    \"consolidated_csv\": \"outputs/2019_2026_CAD_Consolidated_QUALITY_EXCELLENT_96.csv\",\n    \"metadata_json\": \"outputs/consolidation_metadata.json\"\n  },\n  \"records_output\": 231456,\n  \"end_time\": \"2026-01-30T12:32:15\",\n  \"duration_seconds\": 135.42\n}\n\n\n\n3️⃣ SINGLE ENTRYPOINT run_consolidation.py\n\n\n#!/usr/bin/env python3\n# // 2026-01-30-12-35-00 EST\n# // cad_rms_data_quality/run_consolidation.py\n# // Author: R. A. Carucci\n# // Purpose: Single entrypoint for CAD data consolidation pipeline with\n# //          config loading, execution, and formatted summary output\n\n\"\"\"\nCAD Data Consolidation - Single Entrypoint\n\nThis is the main entry point for running the CAD data consolidation pipeline.\nIt handles configuration loading, pipeline execution, and formatted output.\n\nUsage:\n    python run_consolidation.py\n    python run_consolidation.py --dry-run\n    python run_consolidation.py --quality-threshold 90 --output-dir ./output\n\"\"\"\n\nimport argparse\nimport json\nimport logging\nimport sys\nfrom datetime import datetime\nfrom pathlib import Path\nfrom typing import Dict, Any\n\n# Ensure project root is in path\nPROJECT_ROOT = Path(__file__).resolve().parent\nsys.path.insert(0, str(PROJECT_ROOT))\n\nfrom consolidation.scripts.consolidate_cad import (\n    run_consolidation_pipeline,\n    DEFAULT_QUALITY_THRESHOLD,\n)\n\n# Configure logging\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s - %(levelname)s - %(message)s\",\n    handlers=[\n        logging.StreamHandler(sys.stdout),\n        logging.FileHandler(PROJECT_ROOT / \"logs\" / \"consolidation.log\", mode='a'),\n    ]\n)\nlogger = logging.getLogger(__name__)\n\n\ndef print_banner():\n    \"\"\"Print application banner.\"\"\"\n    banner = \"\"\"\n╔══════════════════════════════════════════════════════════════════╗\n║                                                                  ║\n║              CAD/RMS DATA QUALITY SYSTEM v1.0.0                  ║\n║                                                                  ║\n║              Historical CAD Consolidation Pipeline               ║\n║                        (2019-2026)                               ║\n║                                                                  ║\n╚══════════════════════════════════════════════════════════════════╝\n\"\"\"\n    print(banner)\n\n\ndef print_validation_summary(results: Dict[str, Any]):\n    \"\"\"Print formatted validation summary.\"\"\"\n    validation = results.get(\"validation\", {})\n    summary = validation.get(\"summary\", {})\n    \n    print(\"\\n\" + \"─\" * 60)\n    print(\"VALIDATION SUMMARY\")\n    print(\"─\" * 60)\n    print(f\"  Total Records:    {summary.get('total_records', 0):,}\")\n    print(f\"  Valid Records:    {summary.get('valid_records', 0):,}\")\n    print(f\"  Invalid Records:  {summary.get('invalid_records', 0):,}\")\n    print(f\"  Error Count:      {summary.get('error_count', 0)}\")\n    print(f\"  Warning Count:    {summary.get('warning_count', 0)}\")\n    print(f\"  Validation Rate:  {summary.get('validation_rate', 0)}%\")\n    \n    # Field-level issues\n    field_results = validation.get(\"field_results\", {})\n    issues = [(f, r) for f, r in field_results.items() if r.get(\"invalid_count\", 0) > 0]\n    \n    if issues:\n        print(\"\\n  Fields with issues:\")\n        for field, result in sorted(issues, key=lambda x: -x[1].get(\"invalid_count\", 0))[:5]:\n            print(f\"    • {field}: {result.get('invalid_count', 0):,} invalid\")\n\n\ndef print_quality_summary(results: Dict[str, Any]):\n    \"\"\"Print formatted quality score summary.\"\"\"\n    quality = results.get(\"quality\", {})\n    summary = quality.get(\"summary\", {})\n    \n    print(\"\\n\" + \"─\" * 60)\n    print(\"QUALITY SCORE SUMMARY\")\n    print(\"─\" * 60)\n    print(f\"  Mean Score:       {summary.get('mean_score', 0)}/100\")\n    print(f\"  Median Score:     {summary.get('median_score', 0)}/100\")\n    print(f\"  Min/Max:          {summary.get('min_score', 0)} - {summary.get('max_score', 0)}\")\n    print(f\"  Quality Tier:     {summary.get('tier', 'unknown').upper()}\")\n    print(f\"  Pass Rate:        {summary.get('pass_rate', 0)}%\")\n    print(f\"  Threshold:        {summary.get('threshold', 0)}\")\n    \n    # Category breakdown\n    categories = quality.get(\"category_breakdown\", {})\n    if categories:\n        print(\"\\n  Category Breakdown:\")\n        for cat_name, cat_data in categories.items():\n            earned = cat_data.get(\"earned\", 0)\n            max_pts = cat_data.get(\"max\", 0)\n            pct = cat_data.get(\"percentage\", 0)\n            status = \"✓\" if pct >= 80 else \"✗\"\n            print(f\"    {status} {cat_name}: {earned:.1f}/{max_pts:.0f} pts ({pct}%)\")\n    \n    # Flags and recommendations\n    dataset = quality.get(\"dataset\", {})\n    flags = dataset.get(\"flags\", [])\n    recommendations = dataset.get(\"recommendations\", [])\n    \n    if flags:\n        print(\"\\n  Flags:\")\n        for flag in flags[:5]:\n            print(f\"    ⚠ {flag}\")\n    \n    if recommendations:\n        print(\"\\n  Recommendations:\")\n        for rec in recommendations[:5]:\n            print(f\"    → {rec}\")\n\n\ndef print_final_summary(success: bool, results: Dict[str, Any]):\n    \"\"\"Print final pipeline summary.\"\"\"\n    print(\"\\n\" + \"═\" * 60)\n    \n    if success:\n        print(\"✓ PIPELINE COMPLETED SUCCESSFULLY\")\n    else:\n        print(\"✗ PIPELINE FAILED\")\n        if \"failure_reason\" in results:\n            print(f\"  Reason: {results['failure_reason']}\")\n        if \"error\" in results:\n            print(f\"  Error: {results['error']}\")\n    \n    print(\"═\" * 60)\n    print(f\"  Duration:         {results.get('duration_seconds', 0):.2f} seconds\")\n    print(f\"  Records Loaded:   {results.get('records_loaded', 0):,}\")\n    print(f\"  Records Output:   {results.get('records_output', 0):,}\")\n    \n    dedup = results.get(\"deduplication\", {})\n    if dedup:\n        print(f\"  Duplicates:       {dedup.get('duplicates_removed', 0):,} removed\")\n    \n    gaps = results.get(\"gaps\", [])\n    if gaps:\n        print(f\"  Date Gaps:        {len(gaps)} detected\")\n    \n    # Output files\n    output_files = results.get(\"output_files\", {})\n    if output_files and success:\n        print(\"\\n  Output Files:\")\n        for name, path in output_files.items():\n            print(f\"    • {name}: {path}\")\n    \n    print(\"═\" * 60)\n\n\ndef ensure_directories():\n    \"\"\"Ensure required directories exist.\"\"\"\n    dirs = [\n        PROJECT_ROOT / \"logs\",\n        PROJECT_ROOT / \"outputs\" / \"consolidation\",\n    ]\n    for d in dirs:\n        d.mkdir(parents=True, exist_ok=True)\n\n\ndef main():\n    \"\"\"Main entry point.\"\"\"\n    parser = argparse.ArgumentParser(\n        description=\"CAD Data Consolidation Pipeline - Single Entrypoint\",\n        formatter_class=argparse.RawDescriptionHelpFormatter,\n        epilog=\"\"\"\nThis script consolidates historical CAD data (2019-2026) through:\n  1.