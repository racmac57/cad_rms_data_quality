*')\n            allow_null = params.get('allow_null', True)\n            return validate_regex_vectorized(series, pattern, allow_null)\n        \n        elif rule.rule_type == ValidationType.DOMAIN:\n            values = params.get('values', set())\n            case_sensitive = params.get('case_sensitive', False)\n            allow_null = params.get('allow_null', True)\n            partial_match = params.get('partial_match', False)\n            return validate_domain_vectorized(\n                series, values, case_sensitive, allow_null, partial_match\n            )\n        \n        elif rule.rule_type == ValidationType.RANGE:\n            min_val = params.get('min')\n            max_val = params.get('max')\n            allow_null = params.get('allow_null', True)\n            return validate_range_vectorized(series, min_val, max_val, allow_null)\n        \n        elif rule.rule_type == ValidationType.LENGTH:\n            min_len = params.get('min_length')\n            max_len = params.get('max_length')\n            allow_null = params.get('allow_null', True)\n            return validate_length_vectorized(series, min_len, max_len, allow_null)\n        \n        elif rule.rule_type == ValidationType.UNIQUE:\n            validity, _ = validate_unique_vectorized(series)\n            return validity\n        \n        elif rule.rule_type == ValidationType.DATE_FORMAT:\n            formats = params.get('formats')\n            allow_null = params.get('allow_null', True)\n            return validate_date_format_vectorized(series, formats, allow_null)\n        \n        elif rule.rule_type == ValidationType.CUSTOM:\n            # Custom validation function\n            validator = params.get('validator')\n            if callable(validator):\n                return series.apply(validator)\n            return pd.Series([True] * len(series), index=series.index)\n        \n        else:\n            # Unknown rule type - pass all\n            logger.warning(f\"Unknown validation type: {rule.rule_type}\")\n            return pd.Series([True] * len(series), index=series.index)\n    \n    def get_results(self) -> Optional[ValidationResult]:\n        \"\"\"Get the most recent validation results.\"\"\"\n        return self._results\n    \n    def get_summary(self) -> str:\n        \"\"\"Generate human-readable validation summary.\"\"\"\n        if not self._results:\n            return \"No validation has been performed yet.\"\n        \n        r = self._results\n        lines = [\n            \"=\" * 60,\n            \"VALIDATION SUMMARY\",\n            \"=\" * 60,\n            f\"Timestamp:        {r.timestamp}\",\n            f\"Duration:         {r.duration_seconds:.2f} seconds\",\n            f\"Total Records:    {r.total_records:,}\",\n            f\"Valid Records:    {r.valid_records:,}\",\n            f\"Invalid Records:  {r.invalid_records:,}\",\n            f\"Validation Rate:  {r.validation_rate}%\",\n            f\"Fields Validated: {r.fields_validated}\",\n            f\"Rules Applied:    {r.rules_applied}\",\n            f\"Errors:           {r.error_count}\",\n            f\"Warnings:         {r.warning_count}\",\n            \"\",\n            \"FIELD RESULTS:\",\n            \"-\" * 60,\n        ]\n        \n        for field, result in sorted(r.field_results.items()):\n            status = \"✓\" if result.invalid_count == 0 else \"✗\"\n            lines.append(\n                f\"  {status} {field}: {result.valid_count:,}/{result.total_records:,} valid \"\n                f\"({result.validation_rate}%), {result.null_count:,} null\"\n            )\n            if result.error_count > 0:\n                lines.append(f\"      Errors: {result.error_count}\")\n            if result.sample_errors:\n                for err in result.sample_errors[:2]:\n                    lines.append(f\"      - {err.get('message', 'Unknown error')}\")\n        \n        lines.append(\"=\" * 60)\n        \n        return \"\\n\".join(lines)\n\n\n# =============================================================================\n# Convenience Functions\n# =============================================================================\n\ndef validate_dataframe(\n    df: pd.DataFrame,\n    schema: Optional[Dict] = None,\n    rules: Optional[List[ValidationRule]] = None,\n    return_df: bool = True,\n) -> Union[Tuple[pd.DataFrame, Dict], Dict]:\n    \"\"\"\n    Convenience function to validate a DataFrame.\n    \n    Args:\n        df: DataFrame to validate\n        schema: Optional JSON schema\n        rules: Optional custom rules\n        return_df: Whether to return the DataFrame along with results\n        \n    Returns:\n        If return_df: Tuple of (DataFrame, results dict)\n        Otherwise: Just results dict\n    \"\"\"\n    engine = ValidationEngine(rules=rules, schema=schema)\n    results = engine.validate(df)\n    \n    results_dict = results.to_dict()\n    \n    # Log summary\n    logger.info(engine.get_summary())\n    \n    if return_df:\n        return df, results_dict\n    return results_dict\n\n\ndef validate_file(\n    filepath: Path,\n    schema_path: Optional[Path] = None,\n    output_path: Optional[Path] = None,\n) -> Dict:\n    \"\"\"\n    Validate a CAD/RMS export file.\n    \n    Args:\n        filepath: Path to Excel or CSV file\n        schema_path: Optional path to JSON schema\n        output_path: Optional path to save results JSON\n        \n    Returns:\n        Validation results dictionary\n    \"\"\"\n    # Load data\n    filepath = Path(filepath)\n    if filepath.suffix in ['.xlsx', '.xls']:\n        df = pd.read_excel(filepath, dtype=str)\n    else:\n        df = pd.read_csv(filepath, dtype=str)\n    \n    logger.info(f\"Loaded {len(df):,} records from {filepath.name}\")\n    \n    # Load schema if provided\n    schema = None\n    if schema_path:\n        with open(schema_path, 'r', encoding='utf-8') as f:\n            schema = json.load(f)\n    \n    # Validate\n    engine = ValidationEngine(schema=schema)\n    results = engine.validate(df)\n    \n    # Save results if output path provided\n    if output_path:\n        output_path = Path(output_path)\n        with open(output_path, 'w', encoding='utf-8') as f:\n            json.dump(results.to_dict(), f, indent=2)\n        logger.info(f\"Results saved to {output_path}\")\n    \n    # Print summary\n    print(engine.get_summary())\n    \n    return results.to_dict()\n\n\n# =============================================================================\n# CLI Interface\n# =============================================================================\n\ndef main():\n    \"\"\"CLI entry point for validation.\"\"\"\n    parser = argparse.ArgumentParser(\n        description=\"Validate CAD/RMS export files against schema and rules\",\n        formatter_class=argparse.RawDescriptionHelpFormatter,\n        epilog=\"\"\"\nExamples:\n    # Validate with default CAD rules\n    python validation_engine.py --input 2025_CAD.xlsx\n    \n    # Validate with custom schema\n    python validation_engine.py --input data.csv --schema cad_schema.json\n    \n    # Save results to JSON\n    python validation_engine.py --input data.xlsx --output results.json\n        \"\"\"\n    )\n    parser.add_argument(\n        \"--input\", \"-i\",\n        required=True,\n        help=\"Input Excel (.xlsx) or CSV file to validate\",\n    )\n    parser.add_argument(\n        \"--schema\", \"-s\",\n        help=\"Optional JSON schema file for validation rules\",\n    )\n    parser.add_argument(\n        \"--output\", \"-o\",\n        help=\"Optional output path for JSON results\",\n    )\n    parser.add_argument(\n        \"--config\", \"-c\",\n        help=\"Path to validation_rules.yaml for additional rules\",\n    )\n    parser.add_argument(\n        \"--fields\",\n        nargs=\"+\",\n        help=\"Specific fields to validate\",\n    )\n    parser.add_argument(\n        \"--verbose\", \"-v\",\n        action=\"store_true\",\n        help=\"Enable debug logging\",\n    )\n    parser.add_argument(\n        \"--json\",\n        action=\"store_true\",\n        help=\"Output results as JSON to stdout\",\n    )\n    \n    args = parser.parse_args()\n    \n    # Configure logging\n    log_level = logging.DEBUG if args.verbose else logging.INFO\n    logging.basicConfig(\n        level=log_level,\n        format=\"%(asctime)s - %(levelname)s - %(message)s\",\n    )\n    \n    # Load input file\n    input_path = Path(args.input)\n    if not input_path.exists():\n        logger.error(f\"Input file not found: {input_path}\")\n        sys.exit(1)\n    \n    if input_path.suffix in ['.xlsx', '.xls']:\n        df = pd.read_excel(input_path, dtype=str)\n    else:\n        df = pd.read_csv(input_path, dtype=str)\n    \n    logger.info(f\"Loaded {len(df):,} records with {len(df.columns)} columns\")\n    \n    # Load schema if provided\n    schema = None\n    if args.schema:\n        schema_path = Path(args.schema)\n        if schema_path.exists():\n            with open(schema_path, 'r', encoding='utf-8') as f:\n                schema = json.load(f)\n            logger.info(f\"Loaded schema from {schema_path}\")\n        else:\n            logger.warning(f\"Schema file not found: {schema_path}\")\n    \n    # Load config if provided\n    config_path = None\n    if args.config:\n        config_path = Path(args.config)\n        if not config_path.exists():\n            logger.warning(f\"Config file not found: {config_path}\")\n            config_path = None\n    \n    # Create engine and validate\n    engine = ValidationEngine(schema=schema, config_path=config_path)\n    results = engine.validate(df, fields=args.fields)\n    \n    # Output results\n    if args.json:\n        print(json.dumps(results.to_dict(), indent=2))\n    else:\n        print(engine.get_summary())\n    \n    # Save to file if requested\n    if args.output:\n        output_path = Path(args.output)\n        with open(output_path, 'w', encoding='utf-8') as f:\n            json.dump(results.to_dict(), f, indent=2, default=str)\n        logger.info(f\"Results saved to {output_path}\")\n    \n    # Exit with appropriate code\n    if results.error_count > 0:\n        sys.exit(1)\n    sys.exit(0)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n\nKey Features Implemented\n\nFeature\nImplementation\nRequired field validation\nvalidate_required_vectorized() - checks null, empty, whitespace\nRegex pattern compliance\nvalidate_regex_vectorized() - case number YY-NNNNNN format\nDomain/vocabulary enforcement\nvalidate_domain_vectorized() - HowReported, Disposition values\nRange validation\nvalidate_range_vectorized() - numeric min/max bounds\nLength validation\nvalidate_length_vectorized() - string length constraints\nUniqueness checks\nvalidate_unique_vectorized() - duplicate detection\nDate format validation\nvalidate_date_format_vectorized() - multiple format support\nSchema-driven rules\n_build_rules_from_schema() - auto-generate from JSON\nConfig loading\n_load_rules_from_config() - validation_rules.yaml support\nParallel processing ready\nThreadPoolExecutor/ProcessPoolExecutor hooks\nComprehensive metrics\nValidationResult and FieldValidationResult dataclasses\nCLI interface\n--input, --schema, --output, --fields, --json flags\n\nUsage in consolidate_cad.py\n\n\nReplace the stub import with:\n\n\nfrom shared.validators.validation_engine import ValidationEngine, validate_dataframe\n\n\nThe call signature matches the existing stub:\n\n\ndf_validated, validation_results = validate_dataframe(df_normalized, cad_schema)\n\n\nCLI Examples\n\n\n# Validate with default CAD rules\npython -m shared.validators.validation_engine --input 2025_CAD.xlsx\n\n# Validate with custom schema\npython -m shared.validators.validation_engine -i data.csv -s cad_schema.json\n\n# Save JSON results\npython -m shared.validators.validation_engine -i data.xlsx -o results.json --json\n\n# Validate specific fields only\npython -m shared.validators.validation_engine -i data.xlsx --fields ReportNumberNew HowReported"
    },
    {
      "role": "Prompt",
      "time": "1/30/2026, 2:51:46 PM",
      "say": "Amazing Job, I am beyond impressed. Please complete the pipeline \nYou’ve already reviewed the PLAN.md and implemented the normalization and validation engines.\n\n\nNow please implement the third stubbed function:\ncalculate_quality_score(df_validated)\n\n\nThis logic should be moved to:\nshared/validators/quality_scorer.py\n\n\nGoals:\n\t•\tInput: the validation results from ValidationEngine\n\t•\tOutput: a per-record and per-dataset quality score (0-100 scale)\n\t•\tUse the PLAN-defined scoring weights:\n\t•\tRequired fields present: 30 pts\n\t•\tValid formats (regex/date): 25 pts\n\t•\tAddress quality (FullAddress2): 20 pts\n\t•\tDomain compliance (e.g. HowReported): 15 pts\n\t•\tConsistency/cross-field logic: 10 pts (stub this part if not ready)\n\t•\tAlso return:\n\t•\tBreakdown of score reasons\n\t•\tFlag if below acceptable threshold\n\t•\tOptional export to CSV/JSON\n\n\nNotes:\n\t•\tQuality scores should be assignable at both record level and dataset level\n\t•\tYou may reuse any ValidationResult structure from the validation engine\n\t•\tInclude summary and histogram of quality distribution"
    },
    {
      "role": "Response",
      "time": "1/30/2026, 2:54:38 PM",
      "say": "# // 2026-01-30-12-00-00 EST\n# // cad_rms_data_quality/shared/validators/quality_scorer.py\n# // Author: R. A. Carucci\n# // Purpose: Quality scoring engine for CAD/RMS data with per-record and dataset-level\n# //          scoring using PLAN-defined weights, threshold flagging, and distribution analysis\n\n\"\"\"\nQuality Scorer Module - Data Quality Assessment Engine\n\nCalculates quality scores for CAD/RMS data at both record and dataset levels using\nthe PLAN-defined scoring weights:\n- Required fields present: 30 pts\n- Valid formats (regex/date): 25 pts  \n- Address quality: 20 pts\n- Domain compliance: 15 pts\n- Consistency/cross-field: 10 pts\n\nUsage:\n    from shared.validators.quality_scorer import QualityScorer, calculate_quality_score\n    \n    scorer = QualityScorer()\n    score_result = scorer.score(df, validation_results)\n    \n    # Or use convenience function\n    score = calculate_quality_score(df, validation_results)\n\"\"\"\n\nimport argparse\nimport json\nimport logging\nimport re\nimport sys\nfrom dataclasses import dataclass, field\nfrom datetime import datetime\nfrom enum import Enum\nfrom pathlib import Path\nfrom typing import Any, Callable, Dict, List, Optional, Set, Tuple, Union\n\nimport numpy as np\nimport pandas as pd\n\nlogger = logging.getLogger(__name__)\n\n\n# =============================================================================\n# Enums and Data Classes\n# =============================================================================\n\nclass QualityTier(Enum):\n    \"\"\"Quality tier classifications based on score ranges.\"\"\"\n    EXCELLENT = \"excellent\"      # 95-100\n    GOOD = \"good\"                # 80-94\n    ACCEPTABLE = \"acceptable\"    # 60-79\n    POOR = \"poor\"                # 40-59\n    CRITICAL = \"critical\"        # 0-39\n    \n    @classmethod\n    def from_score(cls, score: float) -> \"QualityTier\":\n        \"\"\"Determine tier from numeric score.\"\"\"\n        if score >= 95:\n            return cls.EXCELLENT\n        elif score >= 80:\n            return cls.GOOD\n        elif score >= 60:\n            return cls.ACCEPTABLE\n        elif score >= 40:\n            return cls.POOR\n        else:\n            return cls.CRITICAL\n\n\nclass ScoreCategory(Enum):\n    \"\"\"Categories of quality scoring.\"\"\"\n    REQUIRED_FIELDS = \"required_fields\"\n    VALID_FORMATS = \"valid_formats\"\n    ADDRESS_QUALITY = \"address_quality\"\n    DOMAIN_COMPLIANCE = \"domain_compliance\"\n    CONSISTENCY = \"consistency\"\n\n\n@dataclass\nclass CategoryScore:\n    \"\"\"Score breakdown for a single category.\"\"\"\n    category: ScoreCategory\n    max_points: float\n    earned_points: float\n    percentage: float\n    checks_passed: int\n    checks_failed: int\n    details: Dict[str, Any] = field(default_factory=dict)\n    \n    def __post_init__(self):\n        if self.max_points > 0:\n            self.percentage = round(self.earned_points / self.max_points * 100, 2)\n\n\n@dataclass\nclass RecordScore:\n    \"\"\"Quality score for a single record.\"\"\"\n    index: Any\n    total_score: float\n    tier: QualityTier\n    category_scores: Dict[ScoreCategory, CategoryScore]\n    flags: List[str] = field(default_factory=list)\n    \n    def to_dict(self) -> Dict[str, Any]:\n        \"\"\"Convert to dictionary.\"\"\"\n        return {\n            \"index\": self.index,\n            \"total_score\": self.total_score,\n            \"tier\": self.tier.value,\n            \"category_scores\": {\n                k.value: {\n                    \"earned\": v.earned_points,\n                    \"max\": v.max_points,\n                    \"percentage\": v.percentage,\n                }\n                for k, v in self.category_scores.items()\n            },\n            \"flags\": self.flags,\n        }\n\n\n@dataclass\nclass DatasetScore:\n    \"\"\"Quality score for entire dataset.\"\"\"\n    total_records: int\n    mean_score: float\n    median_score: float\n    min_score: float\n    max_score: float\n    std_dev: float\n    tier: QualityTier\n    tier_distribution: Dict[str, int]\n    score_histogram: Dict[str, int]\n    category_averages: Dict[str, float]\n    records_above_threshold: int\n    records_below_threshold: int\n    threshold: float\n    pass_rate: float\n    timestamp: str\n    duration_seconds: float\n    flags: List[str] = field(default_factory=list)\n    recommendations: List[str] = field(default_factory=list)\n    \n    def to_dict(self) -> Dict[str, Any]:\n        \"\"\"Convert to dictionary for JSON serialization.\"\"\"\n        return {\n            \"summary\": {\n                \"total_records\": self.total_records,\n                \"mean_score\": self.mean_score,\n                \"median_score\": self.median_score,\n                \"min_score\": self.min_score,\n                \"max_score\": self.max_score,\n                \"std_dev\": self.std_dev,\n                \"tier\": self.tier.value,\n                \"threshold\": self.threshold,\n                \"pass_rate\": self.pass_rate,\n                \"records_above_threshold\": self.records_above_threshold,\n                \"records_below_threshold\": self.records_below_threshold,\n                \"timestamp\": self.timestamp,\n                \"duration_seconds\": self.duration_seconds,\n            },\n            \"tier_distribution\": self.tier_distribution,\n            \"score_histogram\": self.score_histogram,\n            \"category_averages\": self.category_averages,\n            \"flags\": self.flags,\n            \"recommendations\": self.recommendations,\n        }\n\n\n@dataclass\nclass QualityResult:\n    \"\"\"Complete quality scoring results.\"\"\"\n    dataset_score: DatasetScore\n    record_scores: pd.DataFrame  # DataFrame with per-record scores\n    category_breakdown: Dict[str, CategoryScore]\n    validation_results: Optional[Dict] = None\n    \n    def to_dict(self) -> Dict[str, Any]:\n        \"\"\"Convert to dictionary.\"\"\"\n        return {\n            \"dataset\": self.dataset_score.to_dict(),\n            \"category_breakdown\": {\n                k: {\n                    \"earned\": v.earned_points,\n                    \"max\": v.max_points,\n                    \"percentage\": v.percentage,\n                    \"checks_passed\": v.checks_passed,\n                    \"checks_failed\": v.checks_failed,\n                    \"details\": v.details,\n                }\n                for k, v in self.category_breakdown.items()\n            },\n        }\n\n\n# =============================================================================\n# Default Scoring Configuration\n# =============================================================================\n\n# PLAN-defined scoring weights (must sum to 100)\nDEFAULT_WEIGHTS: Dict[ScoreCategory, float] = {\n    ScoreCategory.REQUIRED_FIELDS: 30.0,\n    ScoreCategory.VALID_FORMATS: 25.0,\n    ScoreCategory.ADDRESS_QUALITY: 20.0,\n    ScoreCategory.DOMAIN_COMPLIANCE: 15.0,\n    ScoreCategory.CONSISTENCY: 10.0,\n}\n\n# Quality thresholds from PLAN\nDEFAULT_THRESHOLDS = {\n    \"consolidated\": 95,    # For historical consolidation\n    \"monthly\": 80,         # For monthly exports\n    \"minimum\": 60,         # Below this is unacceptable\n}\n\n# Required fields for CAD data\nCAD_REQUIRED_FIELDS: List[str] = [\n    \"ReportNumberNew\",\n    \"Incident\",\n    \"TimeOfCall\",\n    \"FullAddress2\",\n    \"PDZone\",\n    \"Disposition\",\n    \"HowReported\",\n]\n\n# Fields requiring format validation\nFORMAT_VALIDATION_FIELDS: Dict[str, str] = {\n    \"ReportNumberNew\": r'^\\d{2}-\\d{6}[A-Z]?$',\n    \"TimeOfCall\": r'^\\d{1,2}:\\d{2}(:\\d{2})?