)\n- Controlled vocabulary enforcement\n- Domain value validation from validation_rules.yaml\n- Type coercion and presence verification\n- Parallelized/vectorized validation for large datasets\n\nUsage:\n    from shared.validators.validation_engine import ValidationEngine, validate_dataframe\n    \n    engine = ValidationEngine()\n    results = engine.validate(df, schema)\n    \n    # Or use convenience function\n    df_validated, results = validate_dataframe(df, schema)\n\"\"\"\n\nimport argparse\nimport json\nimport logging\nimport re\nimport sys\nfrom concurrent.futures import ProcessPoolExecutor, ThreadPoolExecutor\nfrom dataclasses import dataclass, field\nfrom datetime import datetime\nfrom enum import Enum\nfrom pathlib import Path\nfrom typing import Any, Callable, Dict, List, Optional, Set, Tuple, Union\n\nimport numpy as np\nimport pandas as pd\n\nlogger = logging.getLogger(__name__)\n\n\n# =============================================================================\n# Enums and Data Classes\n# =============================================================================\n\nclass ValidationType(Enum):\n    \"\"\"Types of validation checks.\"\"\"\n    REQUIRED = \"required\"                    # Field must be present and non-null\n    REGEX = \"regex\"                          # Value must match regex pattern\n    DOMAIN = \"domain\"                        # Value must be in allowed set\n    RANGE = \"range\"                          # Numeric value in min/max range\n    LENGTH = \"length\"                        # String length constraints\n    TYPE = \"type\"                            # Data type validation\n    CUSTOM = \"custom\"                        # Custom validation function\n    REFERENCE = \"reference\"                  # Cross-field reference validation\n    DATE_FORMAT = \"date_format\"              # Date/time format validation\n    UNIQUE = \"unique\"                        # Values must be unique\n\n\nclass ValidationSeverity(Enum):\n    \"\"\"Severity levels for validation failures.\"\"\"\n    ERROR = \"error\"          # Critical - record is invalid\n    WARNING = \"warning\"      # Non-critical - record usable but flagged\n    INFO = \"info\"            # Informational only\n\n\n@dataclass\nclass ValidationRule:\n    \"\"\"\n    Defines a single validation rule for a field.\n    \n    Attributes:\n        field: Target field name\n        rule_type: Type of validation to perform\n        params: Parameters specific to the rule type\n        severity: How serious a failure is\n        message: Custom error message template\n        enabled: Whether rule is active\n        description: Human-readable description\n    \"\"\"\n    field: str\n    rule_type: ValidationType\n    params: Dict[str, Any] = field(default_factory=dict)\n    severity: ValidationSeverity = ValidationSeverity.ERROR\n    message: str = \"\"\n    enabled: bool = True\n    description: str = \"\"\n    \n    def __post_init__(self):\n        if isinstance(self.rule_type, str):\n            self.rule_type = ValidationType(self.rule_type)\n        if isinstance(self.severity, str):\n            self.severity = ValidationSeverity(self.severity)\n\n\n@dataclass\nclass FieldValidationResult:\n    \"\"\"Validation results for a single field.\"\"\"\n    field: str\n    total_records: int\n    valid_count: int\n    invalid_count: int\n    null_count: int\n    error_count: int = 0\n    warning_count: int = 0\n    validation_rate: float = 0.0\n    rules_applied: List[str] = field(default_factory=list)\n    sample_errors: List[Dict[str, Any]] = field(default_factory=list)\n    \n    def __post_init__(self):\n        if self.total_records > 0:\n            non_null = self.total_records - self.null_count\n            if non_null > 0:\n                self.validation_rate = round(self.valid_count / non_null * 100, 2)\n\n\n@dataclass\nclass ValidationResult:\n    \"\"\"Complete validation results for a DataFrame.\"\"\"\n    total_records: int\n    valid_records: int\n    invalid_records: int\n    error_count: int\n    warning_count: int\n    fields_validated: int\n    rules_applied: int\n    validation_rate: float\n    duration_seconds: float\n    timestamp: str\n    field_results: Dict[str, FieldValidationResult] = field(default_factory=dict)\n    errors: List[Dict[str, Any]] = field(default_factory=list)\n    warnings: List[Dict[str, Any]] = field(default_factory=list)\n    \n    def to_dict(self) -> Dict[str, Any]:\n        \"\"\"Convert to dictionary for JSON serialization.\"\"\"\n        return {\n            \"summary\": {\n                \"total_records\": self.total_records,\n                \"valid_records\": self.valid_records,\n                \"invalid_records\": self.invalid_records,\n                \"error_count\": self.error_count,\n                \"warning_count\": self.warning_count,\n                \"fields_validated\": self.fields_validated,\n                \"rules_applied\": self.rules_applied,\n                \"validation_rate\": self.validation_rate,\n                \"duration_seconds\": self.duration_seconds,\n                \"timestamp\": self.timestamp,\n            },\n            \"field_results\": {\n                k: {\n                    \"total_records\": v.total_records,\n                    \"valid_count\": v.valid_count,\n                    \"invalid_count\": v.invalid_count,\n                    \"null_count\": v.null_count,\n                    \"error_count\": v.error_count,\n                    \"warning_count\": v.warning_count,\n                    \"validation_rate\": v.validation_rate,\n                    \"rules_applied\": v.rules_applied,\n                    \"sample_errors\": v.sample_errors[:5],  # Limit samples\n                }\n                for k, v in self.field_results.items()\n            },\n            \"errors\": self.errors[:100],  # Limit error list\n            \"warnings\": self.warnings[:100],\n        }\n\n\n# =============================================================================\n# Default Validation Rules for CAD Data\n# =============================================================================\n\n# Case number pattern: YY-NNNNNN or YY-NNNNNNA\nCASE_NUMBER_PATTERN = r'^\\d{2}-\\d{6}[A-Z]?$'\n\n# Valid HowReported values\nHOW_REPORTED_DOMAIN: Set[str] = {\n    \"9-1-1\", \"Phone\", \"Walk-In\", \"Self-Initiated\",\n    \"Radio\", \"Online\", \"Referral\", \"Alarm\", \"Other\", \"Transfer\"\n}\n\n# Valid disposition patterns (partial list - actual would come from schema)\nDISPOSITION_DOMAIN: Set[str] = {\n    \"Arrested\", \"Cleared\", \"Dispersed\", \"GOA\", \"No Action Required\",\n    \"Report\", \"Referred\", \"Unfounded\", \"Warning\", \"Citation\", \n    \"Summons\", \"Transported\", \"Cancelled\", \"Assisted\", \"Advised\",\n    \"Unable to Locate\", \"Civil Matter\", \"Completed\",\n}\n\n# PDZone patterns (typically A1, A2, B1, etc. )\nPDZONE_PATTERN = r'^[A-Z]\\d$'\n\n# Time pattern (HH:MM:SS or HH:MM)\nTIME_PATTERN = r'^\\d{1,2}:\\d{2}(:\\d{2})?